import { definePracticeProblem } from "./shared.ts";
import type { SystemDesignPracticeContent } from "./types.ts";

type Seed = Omit<SystemDesignPracticeContent, "timedPlans" | "lastReviewed" | "simpleDiagram" | "scaledDiagram" | "quickReview"> & { simplePath: string; scaledPath: string; coreProblem: string; keyTradeoff: string };
function advanced(seed: Seed) {
  const { simplePath, scaledPath, coreProblem, keyTradeoff, ...problem } = seed;
  return definePracticeProblem({ ...problem, simpleDiagram: simplePath, scaledDiagram: scaledPath, quickReview: { coreProblem, mainDecisions: problem.decisions.slice(0, 3).map((item) => item.option), importantFailure: problem.failures[0].failure, keyTradeoff } });
}
const levels = ["sde2", "senior", "staff"] as const;

export const advancedProductPracticeProblems = [
  advanced({
    id: "video-streaming", title: "YouTube / Video Streaming", summary: "Separate durable upload processing from globally cached adaptive playback while making media state explicit.", prompt: "Design a video upload and on-demand streaming platform.", difficulty: "advanced", category: "Product", estimatedMinutes: 35, roleRelevance: ["backend", "fullstack", "infrastructure"], levelRelevance: levels, concepts: ["requirements", "estimation", "object-storage", "media-processing", "cdn"], prerequisites: ["requirements", "estimation", "object-storage", "cdn"], clarifyingQuestions: ["On-demand only or live streaming too?", "Maximum upload size and processing delay?", "Playback regions, quality levels, and privacy?"], functionalRequirements: ["Upload resumable source video.", "Transcode into playback renditions and publish metadata.", "Stream video with adaptive quality and authorization."], nonFunctionalRequirements: ["Durable source files and resumable processing.", "Low startup latency with high CDN hit rate.", "Playback availability independent from new-upload processing."], outOfScope: ["Recommendation ranking", "Copyright detection implementation", "Live streaming"], capacity: { assumptions: ["5 million uploads/day at 200 MB average", "500 million playback starts/day", "10 minutes average viewing at 3 Mbps"], arithmetic: ["Uploads ≈ 1 PB/day source bytes", "500M / 86,400 ≈ 5.8K playback starts/s average", "One 10-minute view at 3 Mbps ≈ 225 MB; origin must not serve all playback"], decision: "Egress dwarfs request count, so immutable segments behind a CDN are the core scaling move; processing stays asynchronous." }, apis: [{ method: "POST", path: "/v1/videos/uploads", purpose: "Create resumable source upload." }, { method: "POST", path: "/v1/videos/{id}/publish", purpose: "Publish when required renditions are ready." }, { method: "GET", path: "/v1/videos/{id}/playback", purpose: "Return authorized manifest URL and metadata." }], dataModel: [{ entity: "Video", fields: "video_id, owner_id, title, visibility, processing_state, source_key", notes: "State machine separates uploaded, processing, ready, failed, published." }, { entity: "Rendition", fields: "video_id, profile, manifest_key, segment_prefix, checksum", notes: "Renditions and segments are immutable." }], simpleDesign: ["Upload source to object storage.", "One worker transcodes it into a single playable file.", "Playback API returns an authorized object URL."], simplePath: `flowchart LR\n  C[Creator] --> A[Upload API]\n  A --> O[(Object storage)]\n  O --> T[Transcoder]\n  T --> O\n  V[Viewer] --> A\n  A --> O`, scaledPath: `flowchart LR\n  C[Creator] --> U[Multipart upload]\n  U --> S[(Source storage)]\n  S --> Q[Processing queue]\n  Q --> T[Transcode fleet]\n  T --> R[(Renditions and segments)]\n  V[Viewer] --> P[Playback API]\n  P --> D[CDN]\n  D --> R`, criticalFlows: [{ title: "Upload and process", steps: ["Commit source upload and state transition.", "Fan out idempotent rendition jobs.", "Publish manifest only after required outputs validate."] }, { title: "Playback", steps: ["Authorize viewer and return signed manifest.", "Player requests adaptive segments from CDN.", "Origin serves misses from immutable storage."] }], bottlenecks: ["Transcoding consumes bursty CPU/GPU and takes much longer than API calls.", "Serving bytes from origin makes egress and latency unacceptable.", "A popular video can create a cache-miss stampede in one region."], scalingSteps: ["Queue rendition jobs and autoscale isolated processing pools.", "Package video into adaptive segments and cache them globally.", "Pre-warm or request-collapse exceptionally hot new releases."], failures: [{ failure: "A rendition job partially succeeds", impact: "Manifest may reference missing segments.", detection: "Output inventory and checksum validation.", mitigation: "Keep processing state incomplete; retry idempotently by rendition.", tradeoff: "Publication waits for the required set." }, { failure: "CDN region misses a viral video", impact: "Origin receives a sudden egress spike.", detection: "Origin fetch rate and regional cache-hit ratio.", mitigation: "Request collapse, origin shielding, and pre-warm policy.", tradeoff: "Pre-warming consumes bandwidth for uncertain demand." }, { failure: "Source storage event is delivered twice", impact: "Duplicate transcode jobs waste compute.", detection: "Existing job key for video/profile/source version.", mitigation: "Idempotent job creation and output paths.", tradeoff: "Metadata checks add coordination." }], decisions: [{ option: "Precompute several renditions", chooseWhen: "Playback quality and CDN efficiency matter.", cost: "Processing time and storage multiply." }, { option: "Just-in-time transcode", chooseWhen: "Long-tail content is rarely watched.", cost: "First playback latency and compute spikes." }, { option: "Segmented adaptive streaming", chooseWhen: "Networks vary during playback.", cost: "Packaging and player complexity." }], deepDives: ["Upload state machine", "Transcoding orchestration", "CDN playback and hot releases"], mistakes: ["Sending source videos through application memory.", "Making upload wait synchronously for transcoding.", "Treating the CDN as the source of truth."], followUps: ["What changes for live streaming?", "How are private videos protected at the edge?", "How is a new codec rolled out?"], variants: ["Live streaming is added.", "Uploads require safety scanning before publish.", "The product launches globally.", "Creators need near-real-time analytics."], remember: "Keep upload, processing, publication, and playback as separate states and paths. CDN delivery scales immutable segments, not mutable video metadata.", coreProblem: "Asynchronous media processing plus high-egress playback.", keyTradeoff: "Precompute cost versus playback latency and quality.", selfCheck: ["Why does egress dominate?", "When is a video publishable?", "How do duplicate storage events stay safe?", "What protects origin during a viral miss?"], deepDiveOptions: ["Upload", "Transcoding", "CDN", "Adaptive playback", "Live variant"]
  }),
  advanced({
    id: "ride-sharing", title: "Uber / Ride Sharing", summary: "Continuously ingest driver locations, find nearby candidates, and serialize trip state without treating GPS as exact truth.", prompt: "Design a ride-sharing system that matches riders with nearby drivers and tracks an active trip.", difficulty: "advanced", category: "Product", estimatedMinutes: 35, roleRelevance: ["backend", "fullstack"], levelRelevance: levels, concepts: ["requirements", "geospatial-search", "realtime-communication", "sharding"], prerequisites: ["requirements", "geospatial-search", "websockets", "transactions"], clarifyingQuestions: ["Matching only or pricing/navigation too?", "Location update frequency and accuracy?", "Can drivers receive several offers or only one?"], functionalRequirements: ["Ingest current driver location and availability.", "Find and rank nearby candidates.", "Offer, accept, and progress one trip through a state machine."], nonFunctionalRequirements: ["Low matching latency.", "No driver should own two active trips.", "Location may be stale; durable trip state must not be."], outOfScope: ["Route planning", "Dynamic pricing model", "Payment settlement"], capacity: { assumptions: ["2 million online drivers", "One location update every 4 seconds", "200 thousand ride requests/minute at peak"], arithmetic: ["2M / 4 ≈ 500K location updates/s", "200K / 60 ≈ 3.3K match requests/s", "Keeping 10 minutes of 40-byte points ≈ 12 GB raw transient data"], decision: "Location ingest is the dominant write path and can be ephemeral; trip ownership belongs in a transactional durable store." }, apis: [{ method: "POST", path: "/v1/drivers/{id}/locations", purpose: "Update latest location with sequence/time." }, { method: "POST", path: "/v1/rides", purpose: "Create a match request." }, { method: "POST", path: "/v1/rides/{id}/transitions", purpose: "Apply a versioned trip-state transition." }], dataModel: [{ entity: "DriverLocation", fields: "driver_id, geo_cell, lat, lon, observed_at, sequence, availability", notes: "Latest-value, TTL-backed serving state." }, { entity: "Ride", fields: "ride_id, rider_id, driver_id?, state, version, pickup, destination", notes: "Conditional transitions enforce one durable workflow." }, { entity: "Offer", fields: "ride_id, driver_id, expires_at, status", notes: "Short-lived offers make races visible." }], simpleDesign: ["Write latest driver coordinates to one database.", "Query a radius around pickup.", "Transactionally assign the first accepting available driver."], simplePath: `flowchart LR\n  D[Driver app] --> L[(Latest locations)]\n  R[Rider] --> M[Matching service]\n  M --> L\n  M --> T[(Trips)]`, scaledPath: `flowchart LR\n  D[Driver apps] --> G[Location gateways]\n  G --> P[Geo-partitioned location index]\n  R[Rider] --> M[Regional matcher]\n  M --> P\n  M --> O[Offer service]\n  O --> D\n  O --> T[(Transactional trip store)]\n  T --> E[Trip event stream]`, criticalFlows: [{ title: "Location", steps: ["Accept only newer sequence/timestamp.", "Move driver between geo cells.", "Expire silent drivers from availability."] }, { title: "Match", steps: ["Expand nearby cells until enough candidates exist.", "Rank by ETA and product policy.", "Create expiring offers; conditional acceptance claims trip and driver."] }], bottlenecks: ["Raw location writes overwhelm a general-purpose transactional database.", "Dense city cells become hot while rural searches expand widely.", "Concurrent offers race for the same driver."], scalingSteps: ["Partition latest-location state by region and geo cell.", "Coalesce updates and retain only latest values for matching.", "Serialize durable offer acceptance with conditional writes and versions."], failures: [{ failure: "Driver stops sending location", impact: "Matcher may offer a ride to an unavailable driver.", detection: "Location age exceeds TTL.", mitigation: "Remove stale drivers and require offer acknowledgement.", tradeoff: "Short TTL can hide drivers on poor networks." }, { failure: "Two riders receive accepted offers from one driver", impact: "Conflicting trips are created.", detection: "Conditional ownership conflict.", mitigation: "Atomically transition driver availability and ride assignment.", tradeoff: "A regional ownership boundary adds coordination." }, { failure: "Matcher region fails after offer", impact: "Offer and trip status may diverge.", detection: "Expired offer and unmatched ride reconciliation.", mitigation: "Durable offer state and idempotent transition API.", tradeoff: "Recovery may add seconds of delay." }], decisions: [{ option: "Geohash/grid cells", chooseWhen: "Simple partitioning and neighborhood expansion are useful.", cost: "Boundary and density imbalance." }, { option: "Hierarchical spatial index", chooseWhen: "Variable density and larger searches justify it.", cost: "Operational and query complexity." }, { option: "One-at-a-time offers", chooseWhen: "Driver experience and exclusivity matter.", cost: "Longer match latency." }], deepDives: ["Location ingest and TTL", "Geo-cell search", "Offer/accept race"], mistakes: ["Storing every GPS point on the transactional trip path.", "Using a radius query without partition/hot-cell discussion.", "Treating driver availability as a cache-only invariant."], followUps: ["How do airports or stadiums affect matching?", "How is multi-region travel handled?", "What changes for pooled rides?"], variants: ["Drivers update once per second.", "A stadium creates 100K requests.", "Drivers cross a region boundary.", "Multiple riders share a trip."], remember: "Treat location as high-volume, expiring evidence; treat trip assignment as durable state with conditional transitions.", coreProblem: "Geo search over moving candidates plus exclusive trip ownership.", keyTradeoff: "Fresh location accuracy versus write load and battery/network cost.", selfCheck: ["Why can latest location be ephemeral?", "How do cell boundaries affect search?", "Where is double assignment prevented?", "What happens to a stale driver?"], deepDiveOptions: ["Location ingest", "Geo index", "Matching", "Trip state", "Regional ownership"]
  }),
  advanced({
    id: "nearby-search", title: "Yelp / Nearby Search", summary: "Filter and rank places near a point while balancing spatial index precision, metadata filters, and update freshness.", prompt: "Design a nearby place search service with category filters and distance-aware ranking.", difficulty: "advanced", category: "Product", estimatedMinutes: 28, roleRelevance: ["backend", "fullstack"], levelRelevance: levels, concepts: ["requirements", "geospatial-search", "database-indexes", "caching"], prerequisites: ["requirements", "geospatial-search", "database-indexes"], clarifyingQuestions: ["Radius search, viewport search, or both?", "Which filters and ranking signals?", "How quickly must place edits appear?"], functionalRequirements: ["Create/update place metadata and coordinates.", "Search within radius or viewport.", "Filter and rank results with stable pagination."], nonFunctionalRequirements: ["Low query latency.", "High read availability.", "Bounded index freshness with authoritative place metadata."], outOfScope: ["Turn-by-turn navigation", "Review recommendation model", "Map tile rendering"], apis: [{ method: "GET", path: "/v1/places/nearby?lat=...&lon=...&radius=...", purpose: "Search nearby places." }, { method: "PUT", path: "/v1/places/{id}", purpose: "Update authoritative place metadata." }], dataModel: [{ entity: "Place", fields: "place_id, name, lat, lon, category, attributes, version", notes: "Authoritative record validates final visibility." }, { entity: "SpatialDocument", fields: "cell_ids, place_id, filter_terms, rank_signals, version", notes: "Derived search representation can be rebuilt." }], simpleDesign: ["Store places with a spatial database index.", "Run a bounding-box/radius query.", "Filter exact distance and sort candidates."], simplePath: `flowchart LR\n  C[Client] --> A[Nearby API]\n  A --> D[(Spatially indexed places)]`, scaledPath: `flowchart LR\n  W[Place writes] --> D[(Authoritative place DB)]\n  D --> I[Index pipeline]\n  I --> S[(Geo search shards)]\n  C[Clients] --> A[Nearby API]\n  A --> S\n  A --> K[(Query cache)]\n  A --> D`, criticalFlows: [{ title: "Search", steps: ["Map query area to covering cells.", "Fetch filtered candidates from relevant shards.", "Compute exact distance, rank, and return cursor."] }, { title: "Update", steps: ["Commit place version.", "Publish index update.", "Index consumers replace only older versions."] }], bottlenecks: ["Large radius queries touch many cells and candidates.", "Dense city cells become hot.", "Combining many filters can defeat simple spatial indexes."], scalingSteps: ["Use hierarchical cells and cap query radius/candidate count.", "Replicate hot read shards and subdivide dense cells.", "Build a search index with spatial plus filter fields when database queries no longer suffice."], failures: [{ failure: "Index update lags", impact: "A closed or moved place remains visible.", detection: "Index version lag and stale-result reports.", mitigation: "Final authoritative check for sensitive fields and replayable indexing.", tradeoff: "Extra read increases latency." }, { failure: "Dense cell overload", impact: "Popular downtown searches slow down.", detection: "Cell-level candidate and QPS skew.", mitigation: "Subdivide, cache common viewports, and cap work.", tradeoff: "More cells increase merge cost." }, { failure: "Search shard unavailable", impact: "Part of an area has missing results.", detection: "Partial-shard response metadata.", mitigation: "Replicas and explicit partial-result policy.", tradeoff: "Availability may return incomplete results." }], decisions: [{ option: "Database spatial index", chooseWhen: "Filters and scale remain manageable.", cost: "Limited horizontal search flexibility." }, { option: "Dedicated geo search index", chooseWhen: "Read volume, ranking, and filters justify derived infrastructure.", cost: "Freshness lag and rebuild operations." }], deepDives: ["Cell covering and boundaries", "Dense-area hotspots", "Index freshness"], mistakes: ["Using one fixed-size grid everywhere.", "Returning approximate cell distance without exact filtering.", "Calling a search index the source of truth."], followUps: ["How does map viewport search differ?", "How are open-now filters evaluated?", "What happens at the International Date Line?"], variants: ["Users drag a map viewport.", "Open-now must be exact.", "Results include live availability.", "One city dominates traffic."], remember: "Spatial cells narrow candidates; exact geometry and authoritative metadata finish the answer. Density determines how the index must adapt.", coreProblem: "Low-latency filtered spatial candidate retrieval.", keyTradeoff: "Cell precision versus fan-out and hotspot risk.", selfCheck: ["Why use both cell cover and exact distance?", "How does dense geography create hotspots?", "When is a search index justified?"], deepDiveOptions: ["Geo cells", "Filtering", "Ranking", "Freshness"]
  }),
  advanced({
    id: "ticketmaster", title: "Ticketmaster / Reservation System", summary: "Prevent overselling scarce seats through an explicit hold state while absorbing traffic spikes fairly.", prompt: "Design a ticket reservation system for assigned seats and high-demand on-sales.", difficulty: "advanced", category: "Product", estimatedMinutes: 35, roleRelevance: ["backend", "fullstack"], levelRelevance: levels, concepts: ["requirements", "transactions", "isolation-levels", "distributed-locks", "caching"], prerequisites: ["requirements", "transactions", "isolation-levels", "idempotency"], clarifyingQuestions: ["Assigned seats or general admission?", "How long does a hold last?", "Is strict no-oversell required?"], functionalRequirements: ["Browse events and seat availability.", "Place a time-limited hold.", "Confirm purchase exactly once from the user's perspective."], nonFunctionalRequirements: ["Never sell one seat twice.", "Survive extreme on-sale bursts.", "Fair, auditable state transitions and expiry."], outOfScope: ["Dynamic pricing", "Venue operations", "Fraud scoring"], apis: [{ method: "POST", path: "/v1/events/{id}/holds", purpose: "Conditionally hold requested inventory." }, { method: "POST", path: "/v1/holds/{id}/confirm", purpose: "Convert valid hold into order." }, { method: "DELETE", path: "/v1/holds/{id}", purpose: "Release a hold." }], dataModel: [{ entity: "Seat", fields: "event_id, seat_id, state, hold_id?, hold_expires_at?, version", notes: "Conditional update is the inventory correctness boundary." }, { entity: "Hold", fields: "hold_id, user_id, event_id, state, expires_at, idempotency_key", notes: "PENDING → CONFIRMED or EXPIRED/CANCELLED." }, { entity: "Order", fields: "order_id, hold_id, payment_id, status", notes: "Unique hold_id prevents duplicate conversion." }], simpleDesign: ["Keep seats and holds in one relational database.", "Transactionally update AVAILABLE seat rows to HELD with expiry.", "Confirm a valid hold into one order."], simplePath: `stateDiagram-v2\n  [*] --> AVAILABLE\n  AVAILABLE --> HELD: conditional hold\n  HELD --> SOLD: confirm\n  HELD --> AVAILABLE: expire or cancel`, scaledPath: `flowchart LR\n  C[Buyers] --> W[Waiting room and rate limits]\n  W --> R[Reservation service]\n  R --> D[(Partitioned inventory DB)]\n  R --> E[Expiry scheduler]\n  R --> O[Order service]\n  O --> P[Payment provider]\n  D --> V[Availability read model]`, criticalFlows: [{ title: "Hold", steps: ["Validate event and idempotency key.", "Conditionally transition every requested seat in one transaction.", "Create hold with authoritative expiry and return it."] }, { title: "Confirm", steps: ["Verify hold is still HELD and not expired.", "Coordinate payment with idempotent order state.", "Transition seats to SOLD exactly once."] }], bottlenecks: ["A popular event concentrates writes on one inventory partition.", "Refreshing seat maps creates read amplification.", "Expiry and payment ambiguity create state races."], scalingSteps: ["Use admission control/waiting room before inventory.", "Partition by event while preserving transactional seat ownership.", "Serve availability from a derived view but recheck source of truth on hold."], failures: [{ failure: "Hold response is lost after commit", impact: "User retries and may create another hold.", detection: "Same user/idempotency key.", mitigation: "Return existing hold for repeated key.", tradeoff: "Keys require retention through the hold window." }, { failure: "Payment succeeds but confirmation response is lost", impact: "Retry may charge or confirm twice.", detection: "Stable order/payment idempotency identifiers.", mitigation: "Reconcile provider result and make hold-to-order unique.", tradeoff: "User may see pending while reconciliation runs." }, { failure: "Expiry worker is late", impact: "Inventory remains unavailable beyond hold duration.", detection: "Expired HELD rows and scheduler lag.", mitigation: "Check expires_at on every confirm/hold and clean asynchronously.", tradeoff: "Queries and writes must enforce time, not trust the worker." }], decisions: [{ option: "Database conditional update", chooseWhen: "Inventory is naturally transactional and contention is manageable.", cost: "Popular rows/events concentrate database load." }, { option: "Serialized event inventory owner", chooseWhen: "One event needs ordered high-contention commands.", cost: "Ownership/failover complexity and throughput ceiling." }], deepDives: ["Seat hold state machine", "Payment ambiguity", "On-sale admission control"], mistakes: ["Using a cache or distributed lock as inventory truth.", "Releasing seats only through a best-effort timer.", "Showing cached availability as a purchase guarantee."], followUps: ["How does general admission change the model?", "How do multi-seat holds remain atomic?", "How would a fair waiting room work?"], variants: ["Inventory is general admission.", "One million buyers arrive at once.", "Users select adjacent seats.", "Payment may take two minutes."], remember: "The database state transition—not the seat map or lock—is the sale boundary. Holds make time and payment ambiguity explicit.", coreProblem: "Exclusive scarce-inventory ownership under burst traffic.", keyTradeoff: "Strict correctness versus admission throughput and availability.", selfCheck: ["Why is a hold a state, not a cache entry?", "What if the expiry worker is late?", "Where is double-sale prevented?", "Why use a waiting room?"], deepDiveOptions: ["Holds", "Contention", "Payments", "Waiting room", "General admission"]
  }),
  advanced({
    id: "payment-system", title: "Payment System", summary: "Model payment as an idempotent state machine that survives lost responses and reconciles external provider truth.", prompt: "Design a payment orchestration service that charges through external providers and exposes reliable status.", difficulty: "advanced", category: "Product", estimatedMinutes: 35, roleRelevance: ["backend"], levelRelevance: levels, concepts: ["requirements", "transactions", "idempotency", "saga", "retries"], prerequisites: ["requirements", "transactions", "idempotency", "transactional-outbox"], clarifyingQuestions: ["Authorize/capture, immediate charge, refunds?", "One provider or routing across several?", "Which system owns ledger/accounting truth?"], functionalRequirements: ["Create a payment intent idempotently.", "Authorize/capture or charge through a provider.", "Expose status, refund, and reconciliation records."], nonFunctionalRequirements: ["No duplicate business charge from client retries.", "Auditable immutable attempts and state transitions.", "Availability may return pending rather than inventing certainty."], outOfScope: ["Card network implementation", "Fraud model", "Double-entry ledger internals"], apis: [{ method: "POST", path: "/v1/payments", purpose: "Create/execute using Idempotency-Key." }, { method: "GET", path: "/v1/payments/{id}", purpose: "Read authoritative orchestration status." }, { method: "POST", path: "/v1/payments/{id}/refunds", purpose: "Request idempotent refund." }], dataModel: [{ entity: "Payment", fields: "payment_id, merchant_id, order_id, amount, currency, state, version", notes: "Unique merchant/order or idempotency key protects business intent." }, { entity: "Attempt", fields: "attempt_id, payment_id, provider, provider_reference, state, request_hash", notes: "Provider calls and ambiguous outcomes remain auditable." }, { entity: "OutboxEvent", fields: "event_id, aggregate_id, type, payload, published_at?", notes: "Downstream fulfillment observes committed state." }], simpleDesign: ["Persist a payment row, call one provider, and record the result.", "Use a unique idempotency key.", "Return pending when provider outcome is ambiguous."], simplePath: `flowchart LR\n  C[Client] --> A[Payment API]\n  A --> D[(Payment DB)]\n  A --> P[Payment provider]`, scaledPath: `sequenceDiagram\n  participant C as Client\n  participant A as Payment service\n  participant D as Payment DB\n  participant P as Provider\n  participant O as Outbox relay\n  C->>A: create with idempotency key\n  A->>D: claim intent and attempt\n  A->>P: charge with stable reference\n  P-->>A: result or timeout\n  A->>D: transition and write outbox\n  O->>D: publish committed event`, criticalFlows: [{ title: "Charge", steps: ["Claim or replay idempotency result.", "Persist attempt before external call.", "Call provider with stable reference.", "Record definitive result or PENDING_RECONCILIATION."] }, { title: "Reconcile", steps: ["Poll/query provider or process webhook.", "Match stable provider reference.", "Apply only a legal state transition and publish outbox event."] }], bottlenecks: ["Provider latency is outside service control.", "Synchronous downstream fulfillment widens the failure boundary.", "One merchant or sale can create a hot idempotency/account partition."], scalingSteps: ["Keep orchestration stateless and partition payment rows by merchant/payment ID.", "Use outbox events for fulfillment and accounting integrations.", "Isolate providers with budgets, circuits, and explicit routing policy."], failures: [{ failure: "Provider charges but response is lost", impact: "Service cannot safely decide success or retry.", detection: "Timeout with durable attempt and provider reference.", mitigation: "Mark pending and reconcile; never blind-retry with a new reference.", tradeoff: "User confirmation may be delayed." }, { failure: "Database commits but event publication fails", impact: "Fulfillment does not observe successful payment.", detection: "Unpublished outbox rows.", mitigation: "Transactional outbox relay retries idempotently.", tradeoff: "Downstream delivery is at least once." }, { failure: "Client repeats request with different body", impact: "Same key could represent conflicting intent.", detection: "Stored request hash mismatch.", mitigation: "Reject conflicting reuse.", tradeoff: "Clients must scope and manage keys correctly." }], decisions: [{ option: "Synchronous provider call", chooseWhen: "Interactive checkout needs immediate response.", cost: "Provider latency and ambiguity enter request path." }, { option: "Asynchronous payment workflow", chooseWhen: "Methods are slow or callback-driven.", cost: "Clients must handle pending state." }, { option: "Saga-style compensation", chooseWhen: "Several business services coordinate after payment.", cost: "Compensation is not rollback and must be designed per effect." }], deepDives: ["Idempotency claim", "Provider ambiguity and reconciliation", "Outbox and downstream fulfillment"], mistakes: ["Retrying a timed-out charge with a new provider request ID.", "Calling a cache the payment source of truth.", "Publishing success before the payment transaction commits."], followUps: ["How do refunds differ from rollback?", "How is provider failover made safe?", "Where does a ledger fit?"], variants: ["Authorization and capture are separated.", "A bank transfer settles in days.", "Two providers are available.", "Partial refunds are required."], remember: "A timeout is an unknown outcome, not a failure. Stable identities, legal transitions, and reconciliation turn ambiguity into a recoverable state.", coreProblem: "Idempotent external side effects with ambiguous outcomes.", keyTradeoff: "Immediate certainty versus safe pending/reconciliation behavior.", selfCheck: ["Why can a timeout mean success?", "What does the request hash protect?", "Why is outbox delivery still at least once?", "How is a refund different from rollback?"], deepDiveOptions: ["Idempotency", "Provider reconciliation", "Outbox", "Refunds", "Provider routing"]
  }),
] as const;
