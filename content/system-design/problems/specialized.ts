import { definePracticeProblem } from "./shared.ts";
import type { SystemDesignPracticeContent } from "./types.ts";

type Seed = Omit<SystemDesignPracticeContent, "timedPlans" | "lastReviewed" | "simpleDiagram" | "scaledDiagram" | "quickReview"> & { simplePath: string; scaledPath: string; coreProblem: string; keyTradeoff: string };
function specialized(seed: Seed) { const { simplePath, scaledPath, coreProblem, keyTradeoff, ...p } = seed; return definePracticeProblem({ ...p, simpleDiagram: simplePath, scaledDiagram: scaledPath, quickReview: { coreProblem, mainDecisions: p.decisions.map((d) => d.option), importantFailure: p.failures[0].failure, keyTradeoff } }); }
const levels = ["senior", "staff"] as const;

export const specializedPracticeProblems = [
  specialized({
    id: "collaborative-editor", title: "Collaborative Document Editor", summary: "Converge concurrent edits over unreliable connections while preserving a durable operation history, snapshots, and presence.", prompt: "Design a collaborative document editor with simultaneous editing, reconnect, and offline support.", difficulty: "specialized", category: "Product", estimatedMinutes: 32, roleRelevance: ["fullstack", "backend"], levelRelevance: levels, concepts: ["requirements", "collaborative-editing", "realtime-communication", "consistency-models"], prerequisites: ["websockets", "consistency-models", "idempotency"], clarifyingQuestions: ["Plain text, rich text, or structured blocks?", "Online concurrency only or offline edits?", "How many simultaneous editors per document?"], functionalRequirements: ["Open and edit one document concurrently.", "Broadcast operations and collaborator presence.", "Reconnect from a durable version and persist snapshots."], nonFunctionalRequirements: ["Low-latency local editing.", "All accepted operations eventually converge.", "No silent loss across reconnect or service failure."], outOfScope: ["Rendering engine", "Permission product", "Comments and suggestions"], apis: [{ method: "GET", path: "/v1/documents/{id}?afterVersion=...", purpose: "Fetch snapshot plus missing operations." }, { method: "POST", path: "/v1/documents/{id}/operations", purpose: "Submit idempotent versioned operation." }], dataModel: [{ entity: "DocumentSnapshot", fields: "document_id, version, content_ref, created_at", notes: "Checkpoint bounds replay time." }, { entity: "Operation", fields: "document_id, operation_id, base_version, actor_id, payload, committed_version", notes: "Immutable log supports reconnect and audit." }], simpleDesign: ["One collaboration service owns a document session.", "Clients send operations over WebSocket.", "The owner orders operations, persists them, then broadcasts."], simplePath: `flowchart LR\n  C1[Editor A] <--> S[Collaboration service]\n  C2[Editor B] <--> S\n  S --> L[(Operation log)]`, scaledPath: `flowchart LR\n  C[Clients] <--> G[WebSocket gateways]\n  G --> R[Document router]\n  R --> O[Per-document owner]\n  O --> L[(Operation log)]\n  O --> S[(Snapshots)]\n  O --> P[Presence stream]`, criticalFlows: [{ title: "Edit", steps: ["Apply optimistically on client.", "Route operation to document owner.", "Transform/merge against concurrent committed operations.", "Persist, assign version, and broadcast acknowledgement."] }, { title: "Reconnect", steps: ["Client sends last committed version and unsent operation IDs.", "Server returns snapshot/log gap.", "Client rebases pending operations and resubmits idempotently."] }], bottlenecks: ["One hot document concentrates ordering work.", "Operation logs grow without snapshots.", "Presence updates can overwhelm durable edit traffic."], scalingSteps: ["Partition by document and keep a single logical owner per active document.", "Snapshot and compact history while retaining required audit.", "Treat presence as lossy ephemeral traffic separate from operations."], failures: [{ failure: "Owner crashes after persisting but before broadcast", impact: "Clients retry or temporarily miss an operation.", detection: "Operation ID already committed on new owner.", mitigation: "Idempotent operation IDs and reconnect from committed version.", tradeoff: "Clients must reconcile optimistic state." }, { failure: "Client edits offline from old version", impact: "Operations conflict with a long history gap.", detection: "Old base_version.", mitigation: "Rebase with OT/CRDT policy or surface a recoverable conflict.", tradeoff: "Offline convergence raises algorithm and metadata complexity." }, { failure: "Presence gateway fails", impact: "Cursors/users appear stale.", detection: "Session lease expiry.", mitigation: "Expire presence and rebuild on reconnect.", tradeoff: "Presence is approximate by design." }], decisions: [{ option: "Operational Transformation", chooseWhen: "A central ordered server and text operations fit the product.", cost: "Transform functions grow with operation types." }, { option: "CRDT", chooseWhen: "Offline and peer-like merging justify embedded causality metadata.", cost: "Metadata, tombstones, and implementation complexity." }], deepDives: ["OT versus CRDT boundary", "Document ownership", "Snapshot and reconnect"], mistakes: ["Saying CRDT without naming the document/operation model.", "Persisting high-rate presence with edits.", "Broadcasting before durable acceptance without a loss policy."], followUps: ["What changes for rich-text blocks?", "How is a hot document handled?", "How long is operation history retained?"], variants: ["Users edit offline for a week.", "A document has 10K viewers.", "Rich-text structure replaces plain text.", "Audit history cannot be compacted."], remember: "Do not memorize OT or CRDT as a box. Define the operation model, ownership, reconnect contract, and convergence requirement first.", coreProblem: "Convergent concurrent mutation over unreliable sessions.", keyTradeoff: "Central ordering simplicity versus offline merge flexibility.", selfCheck: ["Why separate presence from edits?", "What does an operation ID protect?", "When is a snapshot safe?", "What drives OT versus CRDT?"], deepDiveOptions: ["OT/CRDT", "Reconnect", "Hot documents", "Snapshots", "Presence"]
  }),
  specialized({
    id: "event-analytics", title: "Ad Click / Event Analytics Pipeline", summary: "Ingest immutable events at high rate, compute timely aggregates, and preserve a replayable history for correction.", prompt: "Design a near-real-time event analytics pipeline for clicks and impressions.", difficulty: "specialized", category: "Data", estimatedMinutes: 35, roleRelevance: ["data", "infrastructure", "backend"], levelRelevance: levels, concepts: ["requirements", "event-streaming", "kafka", "flink", "object-storage"], prerequisites: ["event-streaming", "kafka", "batch-vs-streaming"], clarifyingQuestions: ["Peak events per second and retention?", "Dashboard freshness and dimensions?", "Dedupe key and late-event policy?"], functionalRequirements: ["Accept durable event batches.", "Compute near-real-time windowed metrics.", "Query recent aggregates and replay historical raw events."], nonFunctionalRequirements: ["Sustain bursts with backpressure.", "Expose freshness/completeness.", "Handle duplicates and late events deterministically."], outOfScope: ["Ad auction", "Attribution business rules", "Dashboard UI"], capacity: { assumptions: ["5 million events/s peak", "300 bytes/event", "30 days raw hot retention"], arithmetic: ["5M × 300 B ≈ 1.5 GB/s ingress", "≈ 130 TB/day raw", "30 days ≈ 3.9 PB logical before compression/replication"], decision: "A durable partitioned stream absorbs the peak; object storage owns long-term replay while serving stores hold aggregates, not every raw event." }, apis: [{ method: "POST", path: "/v1/events:batch", purpose: "Ingest validated event batch." }, { method: "GET", path: "/v1/metrics?window=...&dimensions=...", purpose: "Read materialized aggregates with watermark." }], dataModel: [{ entity: "Event", fields: "event_id, event_time, type, account_id, campaign_id, attributes", notes: "Stable event ID and event time support dedupe/late handling." }, { entity: "Aggregate", fields: "window, dimension_key, metric, value, revision, watermark", notes: "Upserts/retractions depend on lateness policy." }], simpleDesign: ["Write events to one append log.", "A consumer updates counters in one analytics database.", "Archive raw batches to object storage."], simplePath: `flowchart LR\n  C[Clients] --> I[Ingest]\n  I --> Q[Event log]\n  Q --> W[Aggregator]\n  W --> D[(Analytics DB)]`, scaledPath: `flowchart LR\n  C[Clients] --> G[Ingest gateways]\n  G --> K[Partitioned event stream]\n  K --> F[Stateful stream processing]\n  F --> R[(Real-time store)]\n  K --> O[(Object storage)]\n  O --> B[Batch correction]\n  B --> R`, criticalFlows: [{ title: "Ingest", steps: ["Validate schema and tenant quota.", "Partition by aggregation/ordering key.", "Acknowledge after durable stream commit."] }, { title: "Aggregate", steps: ["Assign event-time windows and dedupe ID within policy horizon.", "Advance watermark and emit/update aggregates.", "Checkpoint state and source offsets together."] }], bottlenecks: ["A single consumer cannot keep up with partitions.", "High-cardinality dimensions explode state.", "Late events force revisions or loss of completeness."], scalingSteps: ["Partition stream and processor state by stable dimension key.", "Enforce dimensional cardinality and approximate only named metrics.", "Tier raw events to object storage for replay/correction."], failures: [{ failure: "Processor restarts after sink write but before checkpoint", impact: "Aggregate update may repeat.", detection: "Repeated window/dimension revision or event ID.", mitigation: "Idempotent/upsert sink and checkpoint-aligned source positions.", tradeoff: "External exactly-once depends on sink semantics." }, { failure: "Events arrive after watermark", impact: "Reported window is incomplete.", detection: "Late-event counters by source.", mitigation: "Allow bounded revisions or route to correction workflow.", tradeoff: "Longer lateness improves completeness but delays finality and holds state." }, { failure: "Ingest exceeds downstream capacity", impact: "Lag and storage grow.", detection: "Partition lag, broker disk, oldest-event age.", mitigation: "Backpressure, quotas, and controlled shedding of explicitly optional events.", tradeoff: "Protection may sacrifice low-value data." }], decisions: [{ option: "Exact dedupe", chooseWhen: "Stable event IDs and bounded horizon justify state.", cost: "Large state and expiry policy." }, { option: "Approximate distinct count", chooseWhen: "Bounded error is acceptable for huge cardinalities.", cost: "Cannot recover exact members/count." }, { option: "Long allowed lateness", chooseWhen: "Completeness matters more than early finality.", cost: "More state and delayed answers." }], deepDives: ["Event-time windows/watermarks", "Dedupe and exactly-once boundary", "Hot dimensions and backpressure"], mistakes: ["Claiming exactly-once end to end without sink analysis.", "Ignoring event time and late data.", "Allowing arbitrary dashboard dimensions without cardinality controls."], followUps: ["How do corrections change aggregates?", "How is a hot campaign handled?", "When is Count-Min Sketch useful?"], variants: ["Results must update in five seconds.", "Events arrive up to 24 hours late.", "Unique users are approximate.", "Raw events must be retained seven years."], remember: "The durable stream owns ordered transport, the processor owns window state, and the sink owns materialized query results. Exactly-once claims stop at system boundaries.", coreProblem: "High-rate durable events into timely correctable aggregates.", keyTradeoff: "Fresh final results versus late-data completeness and state cost.", selfCheck: ["What does a watermark mean?", "Where can duplicates reappear?", "Why archive raw events?", "How does cardinality affect processor state?"], deepDiveOptions: ["Partitioning", "Watermarks", "Dedupe", "Approximation", "Backpressure"]
  }),
  specialized({
    id: "ml-inference-service", title: "ML Inference Service", summary: "Route versioned prediction requests to loaded model replicas while balancing latency, batching, accelerators, and safe fallback.", prompt: "Design an online ML inference service for versioned low-latency predictions.", difficulty: "specialized", category: "ML", estimatedMinutes: 30, roleRelevance: ["ml", "infrastructure"], levelRelevance: levels, concepts: ["requirements", "model-serving", "load-balancing", "caching"], prerequisites: ["model-serving", "load-balancing", "observability"], clarifyingQuestions: ["Latency SLO, throughput, and payload size?", "CPU/GPU and batchability?", "Version rollout, fallback, and consistency requirements?"], functionalRequirements: ["Register and load model versions.", "Route synchronous prediction requests.", "Canary, roll back, and observe versions independently."], nonFunctionalRequirements: ["Meet tail-latency SLO.", "Bound overload and cold-start impact.", "Deterministic version routing and auditable fallback."], outOfScope: ["ML algorithm training", "Feature engineering", "Label generation"], apis: [{ method: "POST", path: "/v1/models/{model}/predict", purpose: "Predict with routing/version context." }, { method: "PUT", path: "/v1/models/{model}/deployments/{version}", purpose: "Configure rollout and capacity." }], dataModel: [{ entity: "ModelVersion", fields: "model, version, artifact_uri, checksum, runtime, status", notes: "Immutable artifact and validated runtime contract." }, { entity: "Deployment", fields: "model, version, traffic_weight, min_replicas, hardware, fallback", notes: "Control plane is separate from prediction data plane." }], simpleDesign: ["One API server loads one model artifact into memory.", "Validate input, run prediction, return model version.", "Measure latency before adding batching or accelerators."], simplePath: `flowchart LR\n  C[Client] --> S[Model server]\n  S --> M[(Loaded model)]`, scaledPath: `flowchart LR\n  C[Clients] --> G[Inference gateway]\n  G --> R[Version router]\n  R --> F[Model server fleet]\n  A[(Artifact store)] --> F\n  F --> B[Dynamic batcher]\n  B --> H[CPU or GPU]\n  O[Telemetry] <-- F`, criticalFlows: [{ title: "Deploy", steps: ["Validate immutable artifact/checksum.", "Warm replicas and run health/canary checks.", "Shift traffic by version weight and retain rollback capacity."] }, { title: "Predict", steps: ["Authenticate, validate, and resolve model version.", "Queue within a latency budget and batch if beneficial.", "Execute, return version, and emit latency/error/model telemetry."] }], bottlenecks: ["Model load time creates cold starts.", "GPU throughput benefits from batching but batching adds queue delay.", "One expensive tenant/request shape can exhaust accelerator capacity."], scalingSteps: ["Keep minimum warm replicas for critical versions.", "Autoscale on queue age/concurrency, not CPU alone.", "Use bounded dynamic batching and admission control per model/tenant."], failures: [{ failure: "New model crashes or violates latency SLO", impact: "Predictions fail or time out.", detection: "Version-scoped canary errors and tail latency.", mitigation: "Stop rollout and route to last healthy version.", tradeoff: "Rollback may reduce prediction quality improvements." }, { failure: "Artifact load fails", impact: "Replica never becomes ready.", detection: "Checksum/load/health failure.", mitigation: "Keep it out of routing and alert deployment controller.", tradeoff: "Available capacity may be lower." }, { failure: "Queue exceeds latency budget", impact: "Requests time out before useful execution.", detection: "Queue age and deadline slack.", mitigation: "Reject early, reduce batch wait, or use documented fallback.", tradeoff: "Shedding protects successful latency at the cost of availability." }], decisions: [{ option: "Dynamic batching", chooseWhen: "Hardware throughput gains exceed allowed queue delay.", cost: "Tail latency and fairness complexity." }, { option: "Dedicated model pools", chooseWhen: "Isolation and predictable loading matter.", cost: "Lower utilization." }, { option: "Shared multi-model servers", chooseWhen: "Many low-traffic models need efficient capacity.", cost: "Memory contention and noisy neighbors." }], deepDives: ["Version routing/canary", "Batching and queue budgets", "Cold starts and autoscaling"], mistakes: ["Teaching model algorithms instead of serving architecture.", "Autoscaling only on CPU for GPU queues.", "Falling back silently without returning version/quality context."], followUps: ["How do large models fit across devices?", "How is shadow traffic handled?", "How are features fetched within latency budget?"], variants: ["P99 must be under 50 ms.", "GPU batching is required.", "Thousands of low-traffic models share capacity.", "The newest model must be canaried at 1%."], remember: "Model serving is versioned resource scheduling under a latency budget. Measure queue time, load time, execution, and fallback separately.", coreProblem: "Low-latency versioned prediction on constrained compute.", keyTradeoff: "Batch throughput and utilization versus queueing latency and isolation.", selfCheck: ["Why autoscale on queue age?", "What makes rollback safe?", "When does batching hurt?", "What metadata should every response expose?"], deepDiveOptions: ["Model loading", "Version routing", "Batching", "Autoscaling", "Fallback"]
  }),
  specialized({
    id: "feature-store", title: "Feature Store", summary: "Produce reproducible offline training data and fresh low-latency online features without training-serving skew.", prompt: "Design a feature store supporting offline training retrieval and online inference lookups.", difficulty: "specialized", category: "ML", estimatedMinutes: 32, roleRelevance: ["ml", "data"], levelRelevance: levels, concepts: ["requirements", "model-serving", "batch-vs-streaming", "object-storage"], prerequisites: ["batch-vs-streaming", "event-time", "model-serving"], clarifyingQuestions: ["Batch, streaming, or both?", "Online latency and freshness?", "Point-in-time training correctness and versioning needs?"], functionalRequirements: ["Define/version feature transformations.", "Materialize offline history and online latest values.", "Retrieve point-in-time training sets and low-latency serving vectors."], nonFunctionalRequirements: ["Training-serving transformation parity.", "Observable freshness and lineage.", "Point-in-time joins must not leak future information."], outOfScope: ["Model training algorithms", "Experiment UI", "Raw data warehouse design"], apis: [{ method: "GET", path: "/v1/online-features/{view}?entities=...", purpose: "Fetch latest online vector." }, { method: "POST", path: "/v1/training-sets", purpose: "Create point-in-time offline dataset." }], dataModel: [{ entity: "FeatureDefinition", fields: "name, version, entity_keys, transformation_ref, ttl, owner", notes: "Versioned contract is shared by batch and stream jobs." }, { entity: "FeatureValue", fields: "entity_key, feature_version, event_time, value, produced_at", notes: "Event time enables point-in-time correctness." }], simpleDesign: ["Compute features in one batch pipeline into warehouse tables.", "Export latest values to a key-value store for serving.", "Share transformation code/version metadata."], simplePath: `flowchart LR\n  R[Raw data] --> B[Batch transform]\n  B --> O[(Offline table)]\n  B --> N[(Online KV)]`, scaledPath: `flowchart LR\n  R[Raw data] --> B[Batch pipelines]\n  R --> S[Streaming pipelines]\n  B --> O[(Offline store)]\n  S --> O\n  S --> N[(Online store)]\n  C[Feature registry] --> B\n  C --> S\n  T[Training builder] --> O\n  I[Inference] --> N`, criticalFlows: [{ title: "Materialize", steps: ["Resolve versioned definition and event-time source.", "Compute values in batch/stream path.", "Write offline history and latest online value with version/freshness."] }, { title: "Training set", steps: ["Take labeled entity timestamps.", "Join each feature using only values available at that time.", "Record definitions and source snapshots for reproducibility."] }], bottlenecks: ["Dual pipelines drift in logic.", "High-cardinality entities pressure online storage.", "Backfills can overwrite fresh online values without version/time guards."], scalingSteps: ["Centralize feature definitions and generated/shared transforms where feasible.", "Partition offline by time/entity and online by entity hash.", "Gate writes by event time/version and isolate backfill resources."], failures: [{ failure: "Streaming pipeline lags", impact: "Online predictions use stale features.", detection: "Feature freshness by definition/entity cohort.", mitigation: "Expose age, use TTL/default policy, and recover from durable source.", tradeoff: "Fallback may reduce model quality." }, { failure: "Batch and online transforms differ", impact: "Training-serving skew degrades predictions.", detection: "Parity tests and sampled offline/online comparison.", mitigation: "Shared definition/version and continuous skew checks.", tradeoff: "Shared abstractions constrain custom transforms." }, { failure: "Backfill contains future data", impact: "Training metrics are invalid due to leakage.", detection: "Point-in-time audit and temporal invariants.", mitigation: "As-of joins on event time and immutable source snapshots.", tradeoff: "Correct joins are more expensive." }], decisions: [{ option: "Separate offline and online stores", chooseWhen: "Historical scans and millisecond key lookups both matter.", cost: "Materialization and consistency complexity." }, { option: "Batch-only features", chooseWhen: "Daily freshness is enough.", cost: "Cannot support real-time signals." }, { option: "Streaming materialization", chooseWhen: "Freshness changes prediction value.", cost: "State, late data, and operations." }], deepDives: ["Point-in-time joins", "Training-serving skew", "Backfill versus streaming writes"], mistakes: ["Joining training rows to the latest feature value.", "Building separate undocumented transforms online and offline.", "Serving values without freshness/version metadata."], followUps: ["How are on-demand request features combined?", "How do schema changes roll out?", "How is a giant backfill isolated?"], variants: ["Features update daily.", "Fraud features must update in seconds.", "GDPR deletion is required.", "A model needs the last 100 events per user."], remember: "A feature store is a consistency and reproducibility layer around transformations—not just two databases.", coreProblem: "Consistent time-aware features for training and serving.", keyTradeoff: "Fresh online values versus reproducible offline history and pipeline complexity.", selfCheck: ["What is point-in-time correctness?", "How does future leakage occur?", "What detects training-serving skew?", "Why retain event time and version?"], deepDiveOptions: ["Point-in-time joins", "Online store", "Streaming", "Backfills", "Lineage"]
  }),
  specialized({
    id: "vector-search", title: "Vector Search Service", summary: "Ingest versioned embeddings, search approximate nearest-neighbor indexes, and combine metadata filters with safe rebuilds.", prompt: "Design a vector similarity search service with document updates, metadata filtering, and model-version migration.", difficulty: "specialized", category: "ML", estimatedMinutes: 32, roleRelevance: ["ml", "data", "backend"], levelRelevance: levels, concepts: ["requirements", "vector-search", "database-indexes", "caching"], prerequisites: ["vector-search", "database-indexes", "model-serving"], clarifyingQuestions: ["Corpus size, dimensions, and QPS?", "Recall/latency target and filter selectivity?", "How often do embeddings/models change?"], functionalRequirements: ["Ingest/update/delete items and embeddings.", "Query nearest neighbors with metadata filters.", "Rebuild or migrate an index across embedding versions."], nonFunctionalRequirements: ["Bounded search latency and measured recall.", "No mixing incompatible embedding spaces.", "Rebuildable index with deletion and freshness semantics."], outOfScope: ["Training embedding models", "Generative answer synthesis", "Business ranking model"], apis: [{ method: "PUT", path: "/v1/indexes/{index}/items/{id}", purpose: "Upsert source reference, vector, metadata, and model version." }, { method: "POST", path: "/v1/indexes/{index}/search", purpose: "Return nearest candidates with filters and version." }, { method: "DELETE", path: "/v1/indexes/{index}/items/{id}", purpose: "Tombstone an item." }], dataModel: [{ entity: "VectorItem", fields: "item_id, vector_ref, model_version, metadata, source_version, deleted_at?", notes: "Source/version supports re-embedding and deletion." }, { entity: "IndexGeneration", fields: "generation, model_version, algorithm, parameters, status", notes: "One generation contains a compatible embedding space." }], simpleDesign: ["Store vectors in a database and compute exact distance for a small corpus.", "Filter candidates by metadata.", "Return top-K with scores."], simplePath: `flowchart LR\n  I[Items and vectors] --> D[(Vector table)]\n  Q[Query vector] --> E[Exact scan]\n  E --> D`, scaledPath: `flowchart LR\n  I[Documents] --> M[Embedding pipeline]\n  M --> V[(Vector index shards)]\n  M --> D[(Metadata and source store)]\n  Q[Query] --> E[Query embedding]\n  E --> A[ANN search]\n  A --> V\n  A --> F[Filter and rerank]\n  F --> D`, criticalFlows: [{ title: "Index", steps: ["Create embedding using named model version.", "Persist source/metadata version.", "Insert into compatible index generation and publish visibility."] }, { title: "Search", steps: ["Embed query with index model version.", "Apply pre-filter or ANN candidate search.", "Exact-score/rerank bounded candidates and return generation metadata."] }], bottlenecks: ["Exact scan grows linearly with corpus.", "Highly selective post-filters waste ANN candidates.", "Re-embedding the corpus is compute and storage intensive."], scalingSteps: ["Adopt an ANN index only after recall/latency measurement.", "Shard by tenant/semantic partition and replicate read-heavy generations.", "Build a new versioned generation beside the old, then switch atomically."], failures: [{ failure: "Query uses wrong embedding model", impact: "Similarity scores become meaningless.", detection: "Model/index version mismatch.", mitigation: "Route and validate by explicit generation/model version.", tradeoff: "Migrations temporarily run multiple versions." }, { failure: "Delete is absent from ANN graph", impact: "Removed item remains searchable.", detection: "Tombstone/version check at result materialization.", mitigation: "Filter tombstones immediately and rebuild/compact index later.", tradeoff: "Tombstones add query and rebuild cost." }, { failure: "New index generation has poor recall", impact: "Relevant items disappear.", detection: "Offline benchmark and shadow/canary comparison.", mitigation: "Keep old generation and roll back pointer.", tradeoff: "Dual generations consume storage and compute." }], decisions: [{ option: "Exact search", chooseWhen: "Corpus is small or recall must be exact.", cost: "Latency grows linearly." }, { option: "ANN graph/index", chooseWhen: "Large corpus needs low latency with measurable recall loss.", cost: "Build, memory, tuning, and updates." }, { option: "Pre-filter", chooseWhen: "Filter is selective and index supports it.", cost: "May leave too few candidates or fragment indexes." }], deepDives: ["Exact versus ANN", "Metadata filtering", "Model-version reindex"], mistakes: ["Choosing ANN without a recall target.", "Mixing vectors from different model versions.", "Deleting only from metadata and assuming the index updates instantly."], followUps: ["How do multi-tenant filters affect sharding?", "How are hybrid keyword/vector results combined?", "How is a billion-item reindex rolled out?"], variants: ["The corpus has only 100K items.", "Filters remove 99.9% of candidates.", "Embedding model changes monthly.", "Keyword and vector scores are combined."], remember: "Vector search is a versioned retrieval index. Approximation, filters, and rebuilds must be evaluated with both latency and recall.", coreProblem: "Low-latency similarity retrieval over a changing embedding corpus.", keyTradeoff: "ANN speed versus recall, memory, and update complexity.", selfCheck: ["When is exact search better?", "Why must model version be explicit?", "How do deletes become safe before rebuild?", "What can post-filtering waste?"], deepDiveOptions: ["ANN", "Filtering", "Sharding", "Reindex", "Hybrid retrieval"]
  }),
] as const;
