import { definePracticeProblem } from "./shared.ts";
import type { SystemDesignPracticeContent } from "./types.ts";

type Seed = Omit<SystemDesignPracticeContent, "timedPlans" | "lastReviewed" | "simpleDiagram" | "scaledDiagram" | "quickReview"> & {
  simplePath: string;
  scaledPath: string;
  coreProblem: string;
  keyTradeoff: string;
};

function product(seed: Seed) {
  const { simplePath, scaledPath, coreProblem, keyTradeoff, ...problem } = seed;
  return definePracticeProblem({ ...problem, simpleDiagram: simplePath, scaledDiagram: scaledPath, quickReview: {
    coreProblem,
    mainDecisions: problem.decisions.slice(0, 3).map((item) => item.option),
    importantFailure: problem.failures[0].failure,
    keyTradeoff,
  } });
}

const sharedLevels = ["sde2", "senior", "staff"] as const;

export const productPracticeProblems = [
  product({
    id: "notification-service", title: "Notification Service", summary: "Accept notification intent, honor user policy, and deliver through unreliable external channels without losing control of retries.", prompt: "Design a multi-channel notification service for email, SMS, push, and in-app delivery.", difficulty: "intermediate", category: "Product", estimatedMinutes: 25, roleRelevance: ["backend", "fullstack"], levelRelevance: ["sde1", ...sharedLevels], concepts: ["requirements", "message-queues", "pub-sub", "retries", "notification-delivery"], prerequisites: ["requirements", "message-queues", "retries"],
    clarifyingQuestions: ["Transactional, bulk, or both?", "Which preferences and quiet hours apply?", "What does delivered mean for each provider?"], functionalRequirements: ["Accept a notification request with an idempotency key.", "Resolve templates, preferences, and channel routing.", "Track per-channel attempts and terminal outcome."], nonFunctionalRequirements: ["Durable acceptance and controlled retries.", "Priority isolation for transactional traffic.", "Auditable status without promising provider-side receipt."], outOfScope: ["Marketing campaign authoring", "Provider implementation", "Content recommendations"],
    apis: [{ method: "POST", path: "/v1/notifications", purpose: "Accept one logical notification." }, { method: "GET", path: "/v1/notifications/{id}", purpose: "Read logical and channel status." }], dataModel: [{ entity: "Notification", fields: "id, recipient_id, template_id, payload, priority, idempotency_key, status", notes: "One logical request is the source of truth." }, { entity: "DeliveryAttempt", fields: "notification_id, channel, provider_id, attempt, status, next_attempt_at", notes: "Provider retries are independently auditable." }],
    simpleDesign: ["Persist the request before calling one provider.", "A worker renders the template and sends it.", "Record the provider response and expose status."], simplePath: `flowchart LR\n  C[Caller] --> A[Notification API]\n  A --> D[(Notifications)]\n  A --> W[Worker]\n  W --> P[Email provider]`, scaledPath: `flowchart LR\n  C[Callers] --> A[API]\n  A --> D[(Notification store)]\n  A --> Q[Priority queues]\n  Q --> R[Preference and routing]\n  R --> E[Email workers]\n  R --> S[SMS workers]\n  R --> P[Push workers]\n  E --> X[Providers]\n  S --> X\n  P --> X`, criticalFlows: [{ title: "Accept", steps: ["Validate request and idempotency key.", "Persist logical notification and enqueue atomically.", "Return accepted status."] }, { title: "Deliver", steps: ["Resolve current preferences and template.", "Select channel/provider.", "Send with bounded retry and record attempt."] }],
    bottlenecks: ["Synchronous provider calls couple API latency to vendors.", "Bulk traffic can starve password resets.", "Retry storms amplify a provider outage."], scalingSteps: ["Move delivery behind durable priority queues.", "Partition workers by channel and recipient key.", "Use per-provider circuit breakers, retry budgets, and rate limits."], failures: [{ failure: "Provider times out after accepting a send", impact: "A blind retry may duplicate the message.", detection: "Ambiguous provider attempt with request identifier.", mitigation: "Use provider idempotency where available and reconcile status.", tradeoff: "Some channels cannot guarantee dedupe." }, { failure: "Queue backlog grows", impact: "Notifications arrive late.", detection: "Oldest-message age by priority and channel.", mitigation: "Autoscale workers and shed low-priority bulk traffic.", tradeoff: "Prioritization reduces fairness." }, { failure: "Preference service is unavailable", impact: "Delivery policy cannot be confirmed.", detection: "Dependency errors and stale-cache age.", mitigation: "Use a bounded cached snapshot for transactional sends; pause marketing sends.", tradeoff: "Cached preferences may be stale." }], decisions: [{ option: "Queue per priority and channel", chooseWhen: "Isolation and independent scaling matter.", cost: "More routing and operations." }, { option: "Single event stream", chooseWhen: "Replay and multiple independent consumers matter.", cost: "Priority scheduling is less direct." }], deepDives: ["Idempotent acceptance", "Retry and provider fallback", "Preference timing"], mistakes: ["Equating provider acceptance with user delivery.", "Retrying every error indefinitely.", "Letting bulk traffic share an unbounded queue with security messages."], followUps: ["How do quiet hours work across time zones?", "How are templates versioned?", "How is one provider drained?"], variants: ["Password resets must arrive within 30 seconds.", "Users opt out while a notification is queued.", "One provider has a regional outage.", "A campaign sends to 50 million users."], remember: "Persist intent first, then isolate delivery by priority and channel. External provider ambiguity makes idempotency and reconciliation central.", coreProblem: "Durable, policy-aware multi-channel delivery.", keyTradeoff: "Fast asynchronous acceptance versus eventual delivery status.", selfCheck: ["Why separate logical notification from attempts?", "How do you contain retry storms?", "What does delivered mean for SMS?"], deepDiveOptions: ["Provider routing", "Retries", "Preferences", "Bulk isolation"]
  }),
  product({
    id: "search-autocomplete", title: "Search Autocomplete", summary: "Return useful prefix suggestions in tens of milliseconds while popularity changes and abusive terms are filtered.", prompt: "Design search autocomplete for a large consumer product.", difficulty: "intermediate", category: "Product", estimatedMinutes: 22, roleRelevance: ["backend", "fullstack", "data"], levelRelevance: ["sde1", ...sharedLevels], concepts: ["requirements", "search-autocomplete", "caching", "data-modeling"], prerequisites: ["requirements", "caching"], clarifyingQuestions: ["Prefixes, typo tolerance, or both?", "Are results global or personalized?", "How fresh must trending queries be?"], functionalRequirements: ["Return ranked suggestions for a prefix.", "Ingest query popularity signals.", "Filter blocked or unsafe suggestions."], nonFunctionalRequirements: ["Very low read latency.", "High read availability.", "Bounded ranking freshness and deterministic fallback."], outOfScope: ["Full search results", "Semantic retrieval", "Ad placement"], apis: [{ method: "GET", path: "/v1/suggestions?q=eng&limit=10", purpose: "Fetch ranked prefix suggestions." }, { method: "POST", path: "/v1/query-events", purpose: "Record aggregated popularity signals." }], dataModel: [{ entity: "Suggestion", fields: "normalized_text, display_text, score, locale, status", notes: "Ranking state is rebuilt from durable candidates and events." }, { entity: "PrefixTopK", fields: "prefix, locale, suggestion_ids, version", notes: "Materialized top-K makes serving bounded." }], simpleDesign: ["Store suggestions in a table indexed by normalized text.", "Query a prefix range and rank by score.", "Cache common short prefixes."], simplePath: `flowchart LR\n  C[Client] --> A[Suggestion API]\n  A --> D[(Indexed suggestions)]`, scaledPath: `flowchart LR\n  E[Query events] --> P[Aggregation pipeline]\n  P --> B[Top-K builder]\n  B --> S[(Prefix serving index)]\n  C[Clients] --> A[Suggestion API]\n  A --> K[(Prefix cache)]\n  K -->|miss| S`, criticalFlows: [{ title: "Serve", steps: ["Normalize prefix and resolve locale.", "Read cached or materialized top-K.", "Apply safety filters and return suggestions."] }, { title: "Refresh", steps: ["Aggregate query signals over a window.", "Build versioned prefix lists.", "Atomically publish the new serving version."] }], bottlenecks: ["Short prefixes are extremely hot and broad.", "Building every prefix on each request wastes work.", "Frequent trend updates can thrash caches."], scalingSteps: ["Materialize top-K lists for bounded prefix lengths.", "Cache hot prefixes at regional edges.", "Build versioned indexes offline and overlay a small trend delta."], failures: [{ failure: "New index is corrupt", impact: "Suggestions become empty or unsafe.", detection: "Canary quality, size, and blocked-term checks.", mitigation: "Keep previous version and roll back pointer.", tradeoff: "Old rankings remain during recovery." }, { failure: "Event pipeline lags", impact: "Trending suggestions become stale.", detection: "Event-time watermark and index age.", mitigation: "Serve last good index and expose freshness.", tradeoff: "Freshness is sacrificed for availability." }, { failure: "Hot prefix overload", impact: "Serving nodes saturate.", detection: "Prefix-frequency and cache-hit metrics.", mitigation: "Replicate cached top-K and cap prefix work.", tradeoff: "Personalization may be reduced for the hottest prefixes." }], decisions: [{ option: "Trie or finite-state prefix index", chooseWhen: "Fast prefix traversal and compact shared prefixes matter.", cost: "Updates and distribution are more complex." }, { option: "Database prefix range", chooseWhen: "Catalog is modest and simplicity wins.", cost: "Broad prefixes and ranking become expensive." }, { option: "Batch rank plus trend overlay", chooseWhen: "Stable quality and quick trends are both required.", cost: "Two scores must be calibrated." }], deepDives: ["Prefix index layout", "Ranking freshness", "Hot prefixes"], mistakes: ["Running a database LIKE query for every keystroke at global scale.", "Rebuilding and swapping an index without rollback.", "Ignoring locale and blocked-term filtering."], followUps: ["How would typo tolerance change serving?", "How do personalized suggestions remain private?", "How do you remove a banned suggestion quickly?"], variants: ["Suggestions are personalized.", "Trends must appear in one minute.", "Ten locales are supported.", "Typo tolerance is required."], remember: "Precompute bounded top-K results for the read path; move expensive ranking and index construction away from keystrokes.", coreProblem: "Low-latency prefix top-K lookup.", keyTradeoff: "Fresh rankings versus stable precomputed serving.", selfCheck: ["Why are one-letter prefixes dangerous?", "What does a versioned index buy?", "How can trends update without a full rebuild?"], deepDiveOptions: ["Trie/index", "Ranking", "Index publication", "Personalization"]
  }),
  product({
    id: "chat-system", title: "Chat / WhatsApp", summary: "Route ordered conversation messages over long-lived connections, store them durably, and catch up offline devices.", prompt: "Design a one-to-one and small-group chat system with online and offline delivery.", difficulty: "intermediate", category: "Product", estimatedMinutes: 30, roleRelevance: ["backend", "fullstack"], levelRelevance: ["sde1", ...sharedLevels], concepts: ["requirements", "realtime-communication", "message-queues", "pub-sub", "sharding"], prerequisites: ["requirements", "websockets", "message-queues", "idempotency"], clarifyingQuestions: ["One-to-one, groups, or both?", "What ordering guarantee is required?", "Multi-device, edit/delete, presence, and receipts?"], functionalRequirements: ["Establish an authenticated real-time connection.", "Send and receive messages with per-conversation order.", "Store messages for offline catch-up and multiple devices."], nonFunctionalRequirements: ["Low online delivery latency.", "Durable acknowledged messages.", "No global ordering requirement; conversation ordering is explicit."], outOfScope: ["End-to-end encryption internals", "Media transcoding", "Public channel discovery"], capacity: { assumptions: ["50 million daily users", "40 messages/user/day", "500-byte stored message"], arithmetic: ["2B messages/day ≈ 23K writes/s average", "Plan roughly 5× peak ≈ 115K messages/s", "2B × 500 B ≈ 1 TB/day raw message data"], decision: "Message volume and connection count require partitioning by conversation/user and separate connection routing from durable history." }, apis: [{ method: "POST", path: "/v1/conversations/{id}/messages", purpose: "Send with a client message ID." }, { method: "GET", path: "/v1/conversations/{id}/messages?after=cursor", purpose: "Catch up ordered history." }, { method: "POST", path: "/v1/connections", purpose: "Negotiate an authenticated real-time session." }], dataModel: [{ entity: "Message", fields: "conversation_id, sequence, message_id, sender_id, client_message_id, body_ref, created_at", notes: "Conversation plus sequence is the ordered read key." }, { entity: "DeviceCursor", fields: "user_id, device_id, conversation_id, delivered_through, read_through", notes: "Delivery and read state are per device or user by product choice." }], simpleDesign: ["One WebSocket gateway authenticates clients.", "The chat service writes each message to a database.", "If the recipient is connected, the gateway pushes it; otherwise history supplies catch-up."], simplePath: `sequenceDiagram\n  participant S as Sender\n  participant G as Gateway\n  participant C as Chat service\n  participant D as Message DB\n  S->>G: send client_message_id\n  G->>C: authenticated command\n  C->>D: append message\n  D-->>C: sequence committed\n  C-->>G: deliver\n  G-->>S: accepted`, scaledPath: `flowchart LR\n  C[Clients] <--> G[Gateway fleet]\n  G --> P[(Presence registry)]\n  G --> R[Conversation router]\n  R --> M[(Partitioned message store)]\n  R --> B[Delivery broker]\n  B --> G\n  O[Offline sync API] --> M`, criticalFlows: [{ title: "Send", steps: ["Gateway authenticates sender and forwards stable client ID.", "Conversation owner assigns sequence and appends durably.", "Publish delivery after commit; acknowledge sender with server message ID."] }, { title: "Reconnect", steps: ["Client presents last durable cursor.", "Sync API reads messages after cursor.", "Live delivery resumes after the catch-up boundary."] }], bottlenecks: ["A single gateway cannot hold all sockets.", "Conversation ordering can concentrate work for very large groups.", "Fan-out to offline devices can amplify each write."], scalingSteps: ["Partition gateways independently from message ownership.", "Route each conversation to one logical ordering partition.", "Use brokered fan-out and per-device cursors for delivery."], failures: [{ failure: "Message commit succeeds but sender response is lost", impact: "Sender retries and may create a duplicate.", detection: "Same sender and client_message_id.", mitigation: "Unique idempotency key within conversation/sender scope.", tradeoff: "Deduplication records need retention." }, { failure: "Gateway crashes", impact: "Connections drop and presence becomes stale.", detection: "Lease expiry and disconnect metrics.", mitigation: "Clients reconnect, renew presence, and catch up from cursor.", tradeoff: "Presence is intentionally approximate." }, { failure: "Recipient is offline", impact: "Push cannot complete now.", detection: "No active device route.", mitigation: "Keep durable history and optional push notification.", tradeoff: "Delivery latency extends until reconnect." }], decisions: [{ option: "Direct gateway routing", chooseWhen: "Presence is small and online delivery is the dominant path.", cost: "Gateway churn and route updates must be handled." }, { option: "Partitioned delivery broker", chooseWhen: "Buffering and decoupled fan-out are needed.", cost: "Extra hop and duplicate delivery semantics." }, { option: "Conversation ordering", chooseWhen: "Users expect a stable chat sequence.", cost: "Hot conversations limit parallelism." }], deepDives: ["WebSocket connection routing", "Per-conversation ordering", "Offline and multi-device delivery"], mistakes: ["Promising global message ordering.", "Acknowledging before durable storage without stating the loss policy.", "Treating presence as strongly consistent truth."], followUps: ["How do groups with 100K members work?", "What changes with edits and deletes?", "How does multi-region conversation ownership work?"], variants: ["Groups support 100K members.", "Messages may be edited or deleted.", "End-to-end encryption is required.", "Presence must show last-seen time."], remember: "Separate connection routing, durable conversation history, and delivery state. Ordering belongs to a defined scope, usually one conversation.", coreProblem: "Durable ordered messaging over ephemeral connections.", keyTradeoff: "Per-conversation order versus parallel write throughput.", selfCheck: ["Why is client_message_id needed?", "What is presence allowed to get wrong?", "How does reconnect avoid a gap between history and live messages?", "Why not globally order all chat?"], deepDiveOptions: ["Connection routing", "Ordering", "Offline delivery", "Large groups", "Multi-region"]
  }),
  product({
    id: "news-feed", title: "News Feed", summary: "Build a ranked home timeline while controlling fan-out amplification, celebrity traffic, and freshness.", prompt: "Design a social home feed that shows recent ranked posts from followed accounts.", difficulty: "intermediate", category: "Product", estimatedMinutes: 30, roleRelevance: ["backend", "fullstack"], levelRelevance: sharedLevels, concepts: ["requirements", "caching", "sharding", "fan-out", "fanout-read-write"], prerequisites: ["requirements", "caching", "sharding", "fan-out"], clarifyingQuestions: ["Chronological or ranked?", "Maximum follower counts?", "How fresh must new posts and deletions be?"], functionalRequirements: ["Create a post.", "Follow/unfollow accounts.", "Read a paginated home feed."], nonFunctionalRequirements: ["Low read latency.", "Bounded feed freshness.", "High availability with privacy and deletion enforcement."], outOfScope: ["Media processing", "Recommendation-model training", "Ads"], capacity: { assumptions: ["100 million daily users", "2 posts/user/day", "200 feed reads/user/day", "300 followees average"], arithmetic: ["200M posts/day ≈ 2.3K writes/s average", "20B feed reads/day ≈ 231K reads/s average", "Fan-out-on-write at 300 followers averages 60B inbox inserts/day"], decision: "Read pressure favors materialization, but follower skew makes a hybrid fan-out strategy safer than unconditional fan-out-on-write." }, apis: [{ method: "POST", path: "/v1/posts", purpose: "Create a post." }, { method: "GET", path: "/v1/feed?cursor=...", purpose: "Read a stable page of feed candidates." }, { method: "PUT", path: "/v1/follows/{userId}", purpose: "Create a follow edge." }], dataModel: [{ entity: "Post", fields: "post_id, author_id, body_ref, created_at, visibility, deleted_at?", notes: "Author timeline is the durable source." }, { entity: "FeedEntry", fields: "viewer_id, rank_key, post_id, reason, generated_at", notes: "A derived, rebuildable materialized inbox." }, { entity: "Follow", fields: "follower_id, followee_id, created_at", notes: "Shard and index both traversal directions as needed." }], simpleDesign: ["Read followed IDs, fetch their recent posts, merge by time, and return a cursor.", "Keep posts and follow edges in a relational store.", "This fan-out-on-read design is enough before feed reads dominate."], simplePath: `flowchart LR\n  U[User] --> F[Feed API]\n  F --> G[(Follow graph)]\n  F --> P[(Posts by author)]\n  F --> M[Merge recent posts]`, scaledPath: `flowchart LR\n  A[Author post] --> P[(Post store)]\n  A --> Q[Fan-out stream]\n  Q --> W[Feed workers]\n  W --> I[(User inboxes)]\n  C[Reader] --> F[Feed API]\n  F --> I\n  F --> H[Celebrity post merge]\n  H --> P`, criticalFlows: [{ title: "Publish", steps: ["Commit post to author timeline.", "Emit fan-out work.", "Write post references to ordinary followers' inboxes; skip celebrity fan-out."] }, { title: "Read", steps: ["Read materialized inbox candidates.", "Merge recent celebrity posts on demand.", "Filter privacy/deletions and rank before cursoring."] }], bottlenecks: ["Fan-out-on-read makes every feed read expensive.", "Fan-out-on-write multiplies writes by follower count.", "Celebrity accounts create extreme skew and slow deletion propagation."], scalingSteps: ["Materialize inboxes for ordinary accounts.", "Use a hybrid read merge for high-fan-out authors.", "Cache pages briefly while applying final privacy/deletion filters."], failures: [{ failure: "Fan-out workers lag", impact: "New posts appear late.", detection: "Queue age and generated_at freshness.", mitigation: "Serve author-timeline fallback for recent high-priority posts.", tradeoff: "Fallback increases read work." }, { failure: "Duplicate fan-out event", impact: "Post appears twice.", detection: "Repeated post/viewer key.", mitigation: "Use idempotent feed-entry upsert.", tradeoff: "Unique keys add write amplification." }, { failure: "Deleted post remains in inboxes", impact: "Stale references survive.", detection: "Tombstone/filter metrics.", mitigation: "Treat post visibility as authoritative at read time and clean asynchronously.", tradeoff: "Final filtering costs read CPU." }], decisions: [{ option: "Fan-out-on-write", chooseWhen: "Follower counts are manageable and very fast reads matter.", cost: "Write amplification and celebrity skew." }, { option: "Fan-out-on-read", chooseWhen: "Write amplification would be enormous or feeds are rarely opened.", cost: "Heavy, variable reads." }, { option: "Hybrid", chooseWhen: "The follower distribution has a long tail.", cost: "Two candidate paths and merge logic." }], deepDives: ["Hybrid fan-out", "Feed pagination and ranking", "Privacy/deletion propagation"], mistakes: ["Choosing fan-out-on-write without estimating follower skew.", "Caching a feed without a privacy/deletion check.", "Using offset pagination for a changing ranked feed."], followUps: ["How does ranking change the cursor?", "How do private accounts work?", "How is a celebrity post delivered?"], variants: ["The feed is strictly chronological.", "Some authors have 100 million followers.", "Deletes must disappear in seconds.", "Recommended posts are mixed in."], remember: "The central decision is where to pay fan-out cost. A hybrid follows the follower distribution instead of forcing one strategy on every author.", coreProblem: "Low-latency feed assembly under skewed fan-out.", keyTradeoff: "Write amplification versus read amplification.", selfCheck: ["What is the celebrity problem?", "Why is a materialized inbox derived state?", "How should a cursor survive new posts?", "Where is privacy enforced?"], deepDiveOptions: ["Fan-out", "Ranking", "Pagination", "Deletion", "Celebrity traffic"]
  }),
  product({
    id: "job-scheduler", title: "Job Scheduler", summary: "Make delayed and recurring work eligible on time, assign it once at a time, and recover safely from worker failure.", prompt: "Design a distributed scheduler for delayed, recurring, and long-running jobs.", difficulty: "intermediate", category: "Product", estimatedMinutes: 28, roleRelevance: ["backend", "infrastructure"], levelRelevance: sharedLevels, concepts: ["requirements", "job-schedulers", "message-queues", "distributed-locks"], prerequisites: ["requirements", "message-queues", "leases-fencing-tokens"], clarifyingQuestions: ["Delay precision and maximum schedule horizon?", "At-least-once or stronger execution semantics?", "How are retries, cancellation, and long jobs represented?"], functionalRequirements: ["Create one-time or recurring jobs.", "Make due jobs available to workers.", "Track attempts, heartbeats, cancellation, and terminal status."], nonFunctionalRequirements: ["Durable schedules and bounded dispatch delay.", "No concurrent ownership of one attempt.", "Horizontal worker scaling and recoverable leases."], outOfScope: ["Arbitrary user code sandboxing", "Workflow DAG authoring", "Exactly-once external side effects"], apis: [{ method: "POST", path: "/v1/jobs", purpose: "Create a schedule and payload reference." }, { method: "POST", path: "/v1/jobs/{id}/cancel", purpose: "Request cancellation." }, { method: "GET", path: "/v1/jobs/{id}", purpose: "Read schedule and attempt state." }], dataModel: [{ entity: "Job", fields: "job_id, schedule, next_run_at, payload_ref, state, version", notes: "Conditional state transitions protect schedule ownership." }, { entity: "Attempt", fields: "attempt_id, job_id, lease_owner, lease_until, fencing_token, status", notes: "A new attempt/fence supersedes stale workers." }], simpleDesign: ["Poll an indexed jobs table for next_run_at <= now.", "Claim rows with a conditional update.", "Run work and mark completion or retry time."], simplePath: `flowchart LR\n  A[Scheduler poller] --> D[(Jobs by next_run_at)]\n  A --> W[Worker]\n  W --> D`, scaledPath: `flowchart LR\n  C[Clients] --> A[Job API]\n  A --> D[(Schedule store)]\n  T[Time-bucket scanners] --> D\n  T --> Q[Ready queues]\n  Q --> W[Worker fleet]\n  W --> L[(Attempt and lease store)]`, criticalFlows: [{ title: "Dispatch", steps: ["Scan a bounded time bucket for due jobs.", "Conditionally advance schedule and create attempt.", "Enqueue attempt using an outbox or equivalent durable handoff."] }, { title: "Recover", steps: ["Worker heartbeats its lease.", "Expired lease becomes eligible again.", "New owner receives a higher fencing token."] }], bottlenecks: ["Full-table polling degrades as scheduled rows grow.", "One scanner becomes a dispatch bottleneck.", "Slow jobs exhaust workers and hide lease expiry races."], scalingSteps: ["Index and partition by time bucket plus stable shard.", "Separate schedule discovery from ready-work queues.", "Use leases, heartbeats, and fenced side effects for long tasks."], failures: [{ failure: "Scheduler crashes after advancing schedule but before enqueue", impact: "A due execution is lost.", detection: "Schedule/attempt exists without an outbox delivery.", mitigation: "Commit attempt and outbox record atomically.", tradeoff: "At-least-once publishing still requires worker idempotency." }, { failure: "Worker pauses past lease", impact: "Old and new owners may overlap.", detection: "Expired lease and stale fencing token.", mitigation: "Downstream writes reject older fencing tokens.", tradeoff: "Every protected resource must understand the fence." }, { failure: "Poison job retries forever", impact: "Queue capacity and operators are consumed.", detection: "Attempt count and repeated error signature.", mitigation: "Retry budget, backoff, and dead-letter state.", tradeoff: "Terminal failure may require human repair." }], decisions: [{ option: "Database polling", chooseWhen: "Scale and precision are modest; simplicity matters.", cost: "Polling load and timing granularity." }, { option: "Time buckets plus queues", chooseWhen: "Many schedules need horizontal discovery and dispatch.", cost: "Bucket ownership and durable handoff complexity." }], deepDives: ["Due-job discovery", "Lease/fencing recovery", "Recurring schedule semantics"], mistakes: ["Claiming exactly-once execution across external effects.", "Using a lock without lease-expiry behavior.", "Updating recurring next_run_at without defining missed runs."], followUps: ["What happens across daylight-saving changes?", "How do jobs run for hours?", "How do you pause a tenant?"], variants: ["Dispatch must be accurate within one second.", "Jobs run for six hours.", "Recurring runs missed during downtime must catch up.", "One tenant submits millions of jobs."], remember: "A scheduler separates durable time eligibility from execution. Leases bound ownership; idempotent work handles unavoidable retries.", coreProblem: "Durable time-based eligibility and recoverable execution.", keyTradeoff: "Scheduling precision versus scanning and coordination cost.", selfCheck: ["Why is enqueue after a database claim dangerous?", "What does fencing prevent?", "How do recurring jobs handle downtime?"], deepDiveOptions: ["Time buckets", "Leases", "Retries", "Long-running jobs"]
  }),
  product({
    id: "web-crawler", title: "Web Crawler", summary: "Continuously discover and fetch pages while respecting politeness, suppressing duplicates, and recovering a huge frontier.", prompt: "Design a distributed web crawler for a search indexing pipeline.", difficulty: "intermediate", category: "Product", estimatedMinutes: 28, roleRelevance: ["backend", "data", "infrastructure"], levelRelevance: sharedLevels, concepts: ["requirements", "web-crawling", "message-queues", "rate-limiting"], prerequisites: ["requirements", "message-queues", "rate-limiting"], clarifyingQuestions: ["Which domains and content types?", "Freshness versus breadth?", "How strictly are robots rules and per-host rates enforced?"], functionalRequirements: ["Seed and discover URLs.", "Schedule polite fetches per host.", "Parse content, deduplicate URLs/content, and emit documents."], nonFunctionalRequirements: ["High throughput without harming origin sites.", "Recoverable frontier and fetch attempts.", "Bounded duplicate work and crawl loops."], outOfScope: ["Search ranking", "Browser rendering for every page", "Bypassing access controls"], capacity: { assumptions: ["1 billion known URLs", "100 million fetches/day", "100 KB average response"], arithmetic: ["100M / 86,400 ≈ 1,160 fetches/s average", "100M × 100 KB ≈ 10 TB/day ingress", "A 64-byte frontier record for 1B URLs ≈ 64 GB before indexes"], decision: "Bandwidth and per-host scheduling dominate; partition the frontier by host so politeness remains coordinated." }, apis: [{ method: "POST", path: "/v1/seeds", purpose: "Add approved seed URLs." }, { method: "GET", path: "/v1/crawl/status", purpose: "Read frontier and freshness health." }], dataModel: [{ entity: "UrlState", fields: "canonical_url_hash, url, host, status, next_fetch_at, content_hash", notes: "Canonical URL identity prevents simple duplicates." }, { entity: "HostPolicy", fields: "host, robots_version, next_allowed_at, crawl_delay", notes: "Host-scoped ownership enforces politeness." }], simpleDesign: ["Keep a FIFO URL queue and visited set.", "One fetcher downloads, parses links, and enqueues unseen URLs.", "Rate limit each host."], simplePath: `flowchart LR\n  F[URL frontier] --> W[Fetcher]\n  W --> P[Parser]\n  P --> D[(Visited URLs)]\n  P --> F`, scaledPath: `flowchart LR\n  S[Seeds and discovered links] --> N[Canonicalize and dedupe]\n  N --> F[Frontier partitioned by host]\n  F --> W[Fetcher fleet]\n  W --> P[Parse and extract]\n  P --> O[(Document storage)]\n  P --> N\n  H[(Host policies)] --> F`, criticalFlows: [{ title: "Schedule", steps: ["Canonicalize and check URL identity.", "Route by host partition.", "Release only when host policy permits."] }, { title: "Fetch", steps: ["Claim URL attempt.", "Fetch with size/time limits.", "Store document metadata and emit discovered links with retry outcome."] }], bottlenecks: ["A global FIFO cannot enforce independent host rates fairly.", "Duplicate URLs and redirect loops waste bandwidth.", "Large or slow responses tie up fetch workers."], scalingSteps: ["Partition frontier by host and schedule hosts fairly.", "Use URL fingerprints plus content hashes; probabilistic filters can reduce remote lookups.", "Bound response size, per-host concurrency, retries, and parser work."], failures: [{ failure: "Frontier partition owner crashes", impact: "Some hosts stop crawling or leases are replayed.", detection: "Partition heartbeat and oldest-ready age.", mitigation: "Recover durable frontier and reassign leases.", tradeoff: "Recent URLs may be fetched twice." }, { failure: "Origin is slow or failing", impact: "Workers and retries accumulate.", detection: "Host latency/error rate.", mitigation: "Per-host backoff and circuit state.", tradeoff: "Content freshness for that host declines." }, { failure: "Dedup store is unavailable", impact: "Crawler may flood itself with repeated URLs.", detection: "Dedupe errors and discovery amplification.", mitigation: "Pause discovery admission or use a local probabilistic guard.", tradeoff: "A Bloom filter may suppress some new URLs." }], decisions: [{ option: "URL hash set", chooseWhen: "Exact duplicate suppression fits available storage.", cost: "Remote lookup on every discovered URL." }, { option: "Bloom filter before exact store", chooseWhen: "Most discovered links are repeats and memory efficiency matters.", cost: "False positives can skip unseen pages." }], deepDives: ["Host-partitioned frontier", "Deduplication", "Recrawl freshness"], mistakes: ["Ignoring robots and per-host politeness.", "Using one FIFO for the entire web.", "Fetching unbounded responses or retrying bad hosts aggressively."], followUps: ["How are pages recrawled by importance?", "How do redirects affect canonical identity?", "How is JavaScript-rendered content handled selectively?"], variants: ["News sites need five-minute freshness.", "Only one corporate domain is crawled.", "Content duplicates appear under many URLs.", "Some pages require rendering."], remember: "The frontier is a scheduler, not just a queue. Host ownership makes politeness and fairness enforceable at scale.", coreProblem: "Polite, deduplicated distributed URL scheduling.", keyTradeoff: "Breadth and freshness versus origin safety and bandwidth.", selfCheck: ["Why partition by host?", "What can a Bloom filter get wrong?", "How do retries avoid hammering an origin?", "Where is recrawl priority stored?"], deepDiveOptions: ["Frontier", "Politeness", "Dedupe", "Recrawl"]
  }),
  product({
    id: "cloud-file-storage", title: "Dropbox / Google Drive", summary: "Upload large files reliably, keep metadata authoritative, synchronize versions, and deliver bytes efficiently.", prompt: "Design a cloud file storage and synchronization service.", difficulty: "intermediate", category: "Product", estimatedMinutes: 30, roleRelevance: ["backend", "fullstack", "infrastructure"], levelRelevance: sharedLevels, concepts: ["requirements", "large-file-uploads", "object-storage", "sharding"], prerequisites: ["requirements", "object-storage", "large-file-uploads"], clarifyingQuestions: ["File size limit and folder semantics?", "Version history, sharing, and offline edits?", "Strong consistency for metadata or eventual sync?"], functionalRequirements: ["Create folders and upload/download files.", "Resume large uploads and preserve versions.", "Sync changes across a user's devices and share access."], nonFunctionalRequirements: ["Durable bytes and consistent metadata.", "Efficient large transfers and resumability.", "No unauthorized access through caches or object URLs."], outOfScope: ["Collaborative document editing", "Office rendering", "Cross-provider migration"], capacity: { assumptions: ["50 million active users", "1 GB stored per user average", "20 million 10 MB uploads/day"], arithmetic: ["50M × 1 GB ≈ 50 PB logical storage", "20M × 10 MB ≈ 200 TB/day uploaded", "At 10 chunks/upload, ≈ 2.3K chunk completions/s average"], decision: "Bytes dominate metadata and network cost; direct multipart object transfer avoids proxying every byte through application servers." }, apis: [{ method: "POST", path: "/v1/files/uploads", purpose: "Create multipart upload session." }, { method: "POST", path: "/v1/files/uploads/{id}/complete", purpose: "Commit uploaded chunks as a file version." }, { method: "GET", path: "/v1/changes?cursor=...", purpose: "Sync metadata changes." }], dataModel: [{ entity: "File", fields: "file_id, parent_id, owner_id, name, current_version, deleted_at?", notes: "Metadata and namespace rules are transactional." }, { entity: "FileVersion", fields: "file_id, version, object_key, size, checksum, created_at", notes: "Immutable version points to object bytes." }, { entity: "Change", fields: "owner_scope, sequence, entity_id, operation", notes: "Ordered per-user/team change log drives sync." }], simpleDesign: ["Upload the file through an API into object storage.", "Write one metadata row after bytes succeed.", "List files from metadata and download via authorized object access."], simplePath: `flowchart LR\n  C[Client] --> A[File API]\n  A --> M[(Metadata DB)]\n  A --> O[(Object storage)]`, scaledPath: `flowchart LR\n  C[Clients] --> A[Metadata and upload API]\n  A --> M[(Sharded metadata)]\n  A --> U[Multipart upload session]\n  C --> O[(Object storage)]\n  A --> L[Change log]\n  L --> S[Device sync]\n  O --> D[CDN download path]`, criticalFlows: [{ title: "Upload", steps: ["Create authorized upload session and expected checksum.", "Client sends chunks directly to object storage.", "Complete verifies parts and transactionally creates a new metadata version."] }, { title: "Sync", steps: ["Device sends last change cursor.", "Service returns ordered metadata changes.", "Device downloads missing versions and advances cursor."] }], bottlenecks: ["Proxying bytes through API servers wastes bandwidth.", "A global namespace can create metadata contention.", "Many devices polling full directories creates read amplification."], scalingSteps: ["Use direct multipart transfers with resumable sessions.", "Shard metadata by owner/team while keeping namespace operations local.", "Maintain an ordered change log and push a hint rather than full payload."], failures: [{ failure: "All chunks upload but completion request is lost", impact: "Client may retry and create duplicate versions.", detection: "Repeated upload session completion.", mitigation: "Make completion idempotent and return existing version.", tradeoff: "Session state must outlive client retries." }, { failure: "Metadata commit fails after object upload", impact: "Orphaned chunks consume storage.", detection: "Expired uncommitted sessions.", mitigation: "Lifecycle cleanup of unattached upload parts.", tradeoff: "Cleanup is delayed to protect slow clients." }, { failure: "Two devices edit the same base version", impact: "One update may overwrite another.", detection: "Base-version mismatch.", mitigation: "Conditional version update and conflict copy or merge policy.", tradeoff: "Users may resolve conflicts manually." }], decisions: [{ option: "Direct object upload", chooseWhen: "Files are large and application servers should not carry bytes.", cost: "Authorization and completion become multi-step." }, { option: "Content-addressed chunks", chooseWhen: "Deduplication and resumability justify hashing complexity.", cost: "Privacy, reference counting, and garbage collection are harder." }], deepDives: ["Multipart upload commit", "Metadata namespace sharding", "Device sync and conflicts"], mistakes: ["Storing multi-gigabyte files in relational rows.", "Issuing permanent public object URLs.", "Assuming offline edits cannot conflict."], followUps: ["How does cross-user sharing affect sharding?", "How are deleted versions reclaimed?", "How do you deduplicate safely?"], variants: ["Files reach 100 GB.", "Teams share one folder tree.", "Offline clients edit the same file.", "Versions are retained for 30 days."], remember: "Object storage owns immutable bytes; a transactional metadata layer owns namespace, authorization, versions, and sync cursors.", coreProblem: "Durable large-byte transfer plus consistent metadata sync.", keyTradeoff: "Direct scalable transfers versus multi-step commit complexity.", selfCheck: ["Why separate bytes from metadata?", "What makes completion idempotent?", "How does a change cursor beat repeated directory scans?", "Where are conflicts detected?"], deepDiveOptions: ["Multipart uploads", "Sync log", "Conflicts", "Metadata sharding"]
  }),
] as const;
