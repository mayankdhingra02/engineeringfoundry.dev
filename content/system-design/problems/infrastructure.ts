import { definePracticeProblem } from "./shared.ts";
import type { SystemDesignPracticeContent } from "./types.ts";

type Seed = Omit<SystemDesignPracticeContent, "timedPlans" | "lastReviewed" | "simpleDiagram" | "scaledDiagram" | "quickReview"> & { simplePath: string; scaledPath: string; coreProblem: string; keyTradeoff: string };
function infra(seed: Seed) { const { simplePath, scaledPath, coreProblem, keyTradeoff, ...p } = seed; return definePracticeProblem({ ...p, simpleDiagram: simplePath, scaledDiagram: scaledPath, quickReview: { coreProblem, mainDecisions: p.decisions.map((d) => d.option), importantFailure: p.failures[0].failure, keyTradeoff } }); }
const levels = ["senior", "staff"] as const;
const commonOut = ["Billing", "User-facing dashboard design", "Vendor-specific deployment details"] as const;

export const infrastructurePracticeProblems = [
  infra({
    id: "metrics-platform", title: "Metrics / Monitoring Platform", summary: "Ingest high-cardinality time-series data, preserve recent query speed, and keep overload from taking down producers.", prompt: "Design a metrics ingestion, storage, query, and alerting platform.", difficulty: "advanced", category: "Infrastructure", estimatedMinutes: 35, roleRelevance: ["infrastructure", "data", "backend"], levelRelevance: levels, concepts: ["requirements", "metrics", "event-streaming", "time-series-databases", "sharding"], prerequisites: ["requirements", "metrics", "event-streaming", "sharding"], clarifyingQuestions: ["Samples per second and retention tiers?", "Cardinality limits and query shapes?", "Alert evaluation latency?"], functionalRequirements: ["Accept batched metric samples.", "Query time ranges by metric and labels.", "Evaluate alert rules and retain historical aggregates."], nonFunctionalRequirements: ["Durable bounded-lag ingestion.", "Predictable query latency for recent data.", "Explicit cardinality and overload controls."], outOfScope: commonOut, capacity: { assumptions: ["10 million series", "One sample every 10 seconds", "24 bytes compressed sample"], arithmetic: ["10M / 10 ≈ 1M samples/s", "1M × 24 B ≈ 24 MB/s", "24 MB/s × 86,400 ≈ 2.1 TB/day before indexes/replicas"], decision: "The write rate and time-window queries justify partitioning by series and time, batching ingestion, and tiered retention." }, apis: [{ method: "POST", path: "/v1/metrics/write", purpose: "Accept compressed sample batches." }, { method: "GET", path: "/v1/query_range", purpose: "Evaluate expression over a time range." }, { method: "POST", path: "/v1/alerts", purpose: "Create an alert rule." }], dataModel: [{ entity: "Series", fields: "series_id, metric_name, canonical_labels", notes: "Label-set identity and cardinality policy." }, { entity: "SampleBlock", fields: "series_id, window_start, encoded_samples", notes: "Time-window blocks make compression and retention practical." }], simpleDesign: ["Batch writes into one time-series database.", "Index series by canonical labels.", "Run alert queries periodically."], simplePath: `flowchart LR\n  A[Agents] --> I[Ingest API]\n  I --> T[(Time-series DB)]\n  Q[Query and alerts] --> T`, scaledPath: `flowchart LR\n  A[Agents] --> G[Ingest gateways]\n  G --> B[Durable buffer]\n  B --> W[Partitioned writers]\n  W --> H[(Hot time-series store)]\n  W --> C[(Cold object storage)]\n  Q[Query frontend] --> R[Query fan-out]\n  R --> H\n  R --> C\n  E[Rule evaluators] --> R`, criticalFlows: [{ title: "Ingest", steps: ["Validate tenant and cardinality budget.", "Batch, compress, and append to durable buffer.", "Partition by series hash and time window."] }, { title: "Query", steps: ["Resolve label matchers to series.", "Fan out by shard/time tier.", "Merge points, apply aggregation, and return partial-status metadata."] }], bottlenecks: ["Tiny individual writes waste CPU and network.", "Unbounded labels create cardinality explosions.", "Wide time-range queries compete with recent alert queries."], scalingSteps: ["Batch and compress at agents/gateways.", "Shard by series identity and bound label cardinality per tenant.", "Separate hot recent storage from downsampled cold retention."], failures: [{ failure: "Ingestion buffer backlog grows", impact: "Metrics and alerts become stale.", detection: "Event-time lag and oldest batch age.", mitigation: "Backpressure, tenant quotas, and controlled sample dropping.", tradeoff: "Availability may preserve aggregates while losing raw points." }, { failure: "Storage shard fails", impact: "Recent series in that shard are unavailable.", detection: "Replica health and partial query marker.", mitigation: "Replicate blocks and return explicit partial results if needed.", tradeoff: "Replication increases ingest and storage cost." }, { failure: "Cardinality explosion", impact: "Indexes and memory saturate.", detection: "New-series rate by tenant/label.", mitigation: "Reject or aggregate offending series at admission.", tradeoff: "Some diagnostic detail is lost." }], decisions: [{ option: "Push ingestion", chooseWhen: "Short-lived jobs and centralized batching matter.", cost: "Agents must buffer and retry." }, { option: "Pull scraping", chooseWhen: "Target discovery and centralized control fit the environment.", cost: "Network reachability and scrape scheduling." }, { option: "Raw retention", chooseWhen: "Detailed recent debugging is valuable.", cost: "Storage grows rapidly." }], deepDives: ["Series identity and cardinality", "Hot/cold retention", "Query fan-out and partial results"], mistakes: ["Ignoring label cardinality.", "Writing every sample synchronously to object storage.", "Letting dashboard queries starve alerts."], followUps: ["How are late samples handled?", "How do recording rules reduce cost?", "What happens under tenant overload?"], variants: ["Alert lag must be under 10 seconds.", "Retention is 13 months.", "One tenant emits unique request IDs as labels.", "Agents lose network for 15 minutes."], remember: "A metric is a time series identified by its label set. Admission and cardinality controls are as important as storage throughput.", coreProblem: "High-rate time-series ingest plus bounded queries.", keyTradeoff: "Diagnostic detail versus cardinality, retention, and query cost.", selfCheck: ["Why batch samples?", "What creates cardinality explosion?", "How do hot and cold tiers differ?", "When are partial results acceptable?"], deepDiveOptions: ["Cardinality", "Storage layout", "Query fan-out", "Alerting", "Backpressure"]
  }),
  infra({
    id: "distributed-cache", title: "Distributed Cache", summary: "Partition replicated in-memory data while controlling rebalancing, hot keys, eviction, and source-of-truth overload.", prompt: "Design a distributed key-value cache with TTL, eviction, and node failure handling.", difficulty: "advanced", category: "Infrastructure", estimatedMinutes: 32, roleRelevance: ["infrastructure", "backend"], levelRelevance: levels, concepts: ["requirements", "distributed-caching", "consistent-hashing", "replication", "cache-failure-modes"], prerequisites: ["caching", "consistent-hashing", "replication"], clarifyingQuestions: ["Read-through or generic cache API?", "Value sizes and TTL semantics?", "Consistency and acceptable loss on node failure?"], functionalRequirements: ["Get/set/delete values with TTL.", "Evict under memory pressure.", "Add/remove nodes and survive failures."], nonFunctionalRequirements: ["Sub-millisecond to low-millisecond access.", "Even key distribution with hot-key protection.", "Cache failure must not cascade into the source of truth."], outOfScope: commonOut, apis: [{ method: "GET", path: "/v1/cache/{key}", purpose: "Read a value and metadata." }, { method: "PUT", path: "/v1/cache/{key}", purpose: "Set value with TTL and optional version." }, { method: "DELETE", path: "/v1/cache/{key}", purpose: "Invalidate a key." }], dataModel: [{ entity: "CacheEntry", fields: "key, value, expires_at, version, size, access_metadata", notes: "Expiry and eviction are distinct removal reasons." }, { entity: "RingMembership", fields: "node_id, tokens, state, epoch", notes: "Versioned ownership prevents stale routing." }], simpleDesign: ["One in-memory server stores entries with TTL and LRU eviction.", "Clients use get/set/delete.", "A miss goes to the application source of truth."], simplePath: `flowchart LR\n  A[Application] --> C[(Single cache)]\n  A --> D[(Source DB)]`, scaledPath: `flowchart LR\n  A[Clients] --> R[Routing library or proxy]\n  R --> N1[Cache node A]\n  R --> N2[Cache node B]\n  R --> N3[Cache node C]\n  M[Membership epochs] --> R\n  N1 -. replicas .-> N2`, criticalFlows: [{ title: "Get", steps: ["Hash key through current membership epoch.", "Read primary or permitted replica.", "Return miss/expiry explicitly."] }, { title: "Rebalance", steps: ["Publish new membership epoch.", "Stream only moved ranges.", "Use dual-read/forwarding until ownership converges."] }], bottlenecks: ["One node limits memory and throughput.", "Naive modulo hashing remaps most keys when membership changes.", "One hot key saturates its owner even with balanced total memory."], scalingSteps: ["Partition with consistent hashing and virtual nodes.", "Replicate entries only when cache availability warrants it.", "Replicate or locally memoize hot immutable values and collapse misses."], failures: [{ failure: "Cache cluster outage", impact: "All misses hit the source database.", detection: "Hit ratio collapse and origin QPS surge.", mitigation: "Circuit limits, stale fallback, request coalescing, and gradual warm-up.", tradeoff: "Some requests fail or serve stale data to protect the source." }, { failure: "Node leaves during rebalance", impact: "Moved keys miss or route inconsistently.", detection: "Epoch mismatch and range-transfer health.", mitigation: "Versioned membership and bounded dual-read/forwarding.", tradeoff: "Temporary extra reads and memory." }, { failure: "Hot key", impact: "One node reaches CPU/network limit.", detection: "Heavy-hitter sampling.", mitigation: "Replicate hot entry or add near-cache with jittered TTL.", tradeoff: "More copies complicate invalidation." }], decisions: [{ option: "Client-side routing", chooseWhen: "Lowest hop count and trusted clients matter.", cost: "Membership logic and rollout in every client." }, { option: "Proxy routing", chooseWhen: "Centralized policy and simpler clients matter.", cost: "Extra hop and proxy scaling." }, { option: "Replication", chooseWhen: "Miss cost and cache availability justify copies.", cost: "Memory and consistency overhead." }], deepDives: ["Consistent-hash rebalancing", "Eviction/TTL", "Stampede and hot-key protection"], mistakes: ["Calling cache authoritative.", "Adding replicas without a write/invalidation policy.", "Recovering every node at once and stampeding the database."], followUps: ["How do large values affect eviction?", "How is membership distributed?", "What changes for write-through?"], variants: ["Values are immutable for an hour.", "One key gets 20% of traffic.", "The source database is fragile.", "Nodes are added every day."], remember: "A distributed cache is a failure amplifier unless miss traffic, membership change, and hot keys are designed as first-class paths.", coreProblem: "Partitioned low-latency ephemeral storage.", keyTradeoff: "Availability and hit rate versus consistency, memory, and origin protection.", selfCheck: ["Why is modulo hashing disruptive?", "What happens when hit rate falls to zero?", "How do TTL and eviction differ?", "How can a hot key be spread?"], deepDiveOptions: ["Consistent hashing", "Rebalancing", "Eviction", "Hot keys", "Stampede"]
  }),
  infra({
    id: "distributed-queue", title: "Distributed Queue", summary: "Dispatch durable work with leases, acknowledgements, retries, and backpressure while making duplicate delivery explicit.", prompt: "Design a distributed work queue with visibility timeouts and dead-letter handling.", difficulty: "advanced", category: "Infrastructure", estimatedMinutes: 32, roleRelevance: ["infrastructure", "data", "backend"], levelRelevance: levels, concepts: ["requirements", "message-queues", "delivery-semantics", "replication", "backpressure"], prerequisites: ["message-queues", "delivery-semantics", "replication"], clarifyingQuestions: ["Work queue or retained replayable log?", "Ordering scope and delay/priority support?", "Acknowledgement and retention semantics?"], functionalRequirements: ["Enqueue, receive/lease, acknowledge, and retry messages.", "Support delayed delivery and dead-letter policy.", "Partition and replicate durable queue state."], nonFunctionalRequirements: ["At-least-once delivery with explicit ordering scope.", "Bounded loss after acknowledged enqueue.", "Backpressure under slow consumers."], outOfScope: commonOut, apis: [{ method: "POST", path: "/v1/queues/{q}/messages", purpose: "Durably enqueue." }, { method: "POST", path: "/v1/queues/{q}/receive", purpose: "Lease a batch to a consumer." }, { method: "POST", path: "/v1/queues/{q}/messages/{id}/ack", purpose: "Acknowledge successful processing." }], dataModel: [{ entity: "Message", fields: "queue, partition, sequence, payload_ref, visible_at, attempts", notes: "Partition sequence supports local order." }, { entity: "Lease", fields: "message_id, consumer_id, lease_until, receipt_token", notes: "Receipt token rejects stale acknowledgements." }], simpleDesign: ["Store messages in a database table ordered by creation time.", "A worker claims a row until visibility timeout.", "Delete or mark complete on acknowledgement."], simplePath: `flowchart LR\n  P[Producer] --> Q[(Queue table)]\n  Q --> C[Consumer]\n  C --> Q`, scaledPath: `flowchart LR\n  P[Producers] --> B[Queue brokers]\n  B --> L1[Partition leader]\n  L1 -. replicate .-> R1[Followers]\n  L1 --> C[Consumer fleet]\n  C --> A[Ack and lease service]\n  A --> L1\n  L1 --> D[Dead-letter queue]`, criticalFlows: [{ title: "Enqueue", steps: ["Choose partition from ordering key.", "Append and replicate to durability policy.", "Acknowledge producer after commit boundary."] }, { title: "Consume", steps: ["Lease visible messages with receipt tokens.", "Consumer handles effect idempotently.", "Ack before lease ends; expired leases reappear."] }], bottlenecks: ["One ordered queue limits write parallelism.", "Slow consumers grow backlog and retention.", "Large payloads waste broker memory/network."], scalingSteps: ["Partition by ordering key and replicate each partition.", "Store large payloads externally with immutable references.", "Apply producer quotas, bounded retention, and consumer scaling from lag."], failures: [{ failure: "Consumer finishes effect but ack is lost", impact: "Message is delivered again.", detection: "Repeated message/business key.", mitigation: "Idempotent consumer and unique side-effect constraint.", tradeoff: "Exactly-once external effects are not provided by the queue alone." }, { failure: "Partition leader fails", impact: "That partition pauses during election.", detection: "Heartbeat/quorum loss.", mitigation: "Promote in-sync replica and fence old leader.", tradeoff: "Stronger durability raises enqueue latency." }, { failure: "Poison message", impact: "Repeated retries block or consume capacity.", detection: "Attempt threshold and repeated error.", mitigation: "Backoff and dead-letter after bounded attempts.", tradeoff: "DLQ requires inspection and replay tooling." }], decisions: [{ option: "Visibility-timeout work queue", chooseWhen: "Competing consumers should each process one task.", cost: "Replay/history and independent consumer groups are limited." }, { option: "Retained partitioned log", chooseWhen: "Replay and independent groups matter.", cost: "Consumer offsets and partition ordering replace per-message leases." }], deepDives: ["Lease/ack semantics", "Partition replication", "Backpressure and poison handling"], mistakes: ["Promising exactly-once processing.", "Using one partition while requiring huge throughput.", "Keeping multi-megabyte payloads inline without limits."], followUps: ["How are priorities implemented?", "How does delayed delivery scale?", "What happens to an out-of-order retry?"], variants: ["Strict per-customer ordering is required.", "Messages may be 100 MB.", "Consumers are down for a day.", "Some tasks must run at a future time."], remember: "At-least-once delivery follows naturally from recoverable leases. Correct consumers make repeated delivery safe.", coreProblem: "Durable work handoff with recoverable ownership.", keyTradeoff: "Ordering and durability versus parallel throughput and latency.", selfCheck: ["Why can a successful task be delivered twice?", "What does a receipt token protect?", "When should payloads leave the broker?", "How does ordering constrain partitions?"], deepDiveOptions: ["Leases", "Replication", "Ordering", "Delayed work", "Backpressure"]
  }),
  infra({
    id: "key-value-store", title: "Distributed Key-Value Store", summary: "Partition durable records across nodes, replicate them, and expose consistency choices that remain meaningful during failure.", prompt: "Design a distributed key-value database supporting get, put, delete, and horizontal scaling.", difficulty: "advanced", category: "Infrastructure", estimatedMinutes: 38, roleRelevance: ["infrastructure", "backend", "data"], levelRelevance: levels, concepts: ["requirements", "key-value-stores", "consistent-hashing", "replication", "consistency-models"], prerequisites: ["key-value-stores", "consistent-hashing", "replication", "consistency-models"], clarifyingQuestions: ["Value size and access pattern?", "Single-key atomicity only?", "Availability and consistency expectations during partitions?"], functionalRequirements: ["Get, put, and delete opaque values.", "Partition and rebalance key ranges.", "Replicate records and repair divergent replicas."], nonFunctionalRequirements: ["Durability after acknowledged writes.", "Predictable latency under node churn.", "Explicit consistency and conflict semantics."], outOfScope: commonOut, apis: [{ method: "GET", path: "/v1/kv/{key}", purpose: "Read with requested consistency." }, { method: "PUT", path: "/v1/kv/{key}", purpose: "Write value and optional expected version." }, { method: "DELETE", path: "/v1/kv/{key}", purpose: "Write a tombstone." }], dataModel: [{ entity: "Record", fields: "key, value, version/vector, timestamp, tombstone", notes: "Version metadata drives conflict and repair policy." }, { entity: "PartitionMap", fields: "range_or_token, replicas, epoch, state", notes: "Versioned membership identifies current owners." }], simpleDesign: ["One durable server appends writes and maintains an in-memory index.", "Get reads the indexed location.", "Replication begins only after single-node semantics are clear."], simplePath: `flowchart LR\n  C[Client] --> N[Single KV node]\n  N --> L[(Append log)]\n  N --> I[(In-memory index)]`, scaledPath: `flowchart LR\n  C[Client] --> R[Coordinator]\n  R --> M[Partition map]\n  R --> A[Replica A]\n  R --> B[Replica B]\n  R --> C2[Replica C]\n  A -. anti-entropy .-> B\n  B -. repair .-> C2`, criticalFlows: [{ title: "Write", steps: ["Map key to replica set at current epoch.", "Append/version on replicas.", "Acknowledge after configured durability threshold."] }, { title: "Read", steps: ["Read one or several replicas by consistency mode.", "Resolve versions or surface conflict.", "Repair stale replicas asynchronously where safe."] }], bottlenecks: ["One node caps capacity and availability.", "Membership changes move data and compete with foreground traffic.", "Hot keys bypass otherwise even partitioning."], scalingSteps: ["Partition by token/range with virtual ownership.", "Replicate across failure domains and throttle repair/rebalance.", "Split, cache, or specially replicate hot keys."], failures: [{ failure: "Coordinator cannot reach all replicas", impact: "Consistency and availability policy is tested.", detection: "Timeout and replica health.", mitigation: "Use configured quorum/one response or fail explicitly.", tradeoff: "Lower thresholds improve availability but admit staleness/conflicts." }, { failure: "Node rejoins with stale data", impact: "Old versions may be served or overwrite newer state.", detection: "Version comparison and Merkle/repair mismatch.", mitigation: "Fence by epoch and run repair before full service.", tradeoff: "Recovery consumes bandwidth and delays capacity." }, { failure: "Delete misses a replica", impact: "Old value can resurrect.", detection: "Tombstone/version divergence.", mitigation: "Replicate tombstones and retain through repair horizon.", tradeoff: "Tombstones consume space and complicate compaction." }], decisions: [{ option: "Leader-based replication", chooseWhen: "Simple per-key ordering and conflict avoidance matter.", cost: "Leader availability and failover." }, { option: "Leaderless quorum", chooseWhen: "Write availability across replica failures matters.", cost: "Conflicts, repair, and quorum nuance." }, { option: "Range partitioning", chooseWhen: "Ordered scans matter.", cost: "Skew and split management." }], deepDives: ["Partition ownership", "Replication consistency", "Repair/tombstones"], mistakes: ["Quoting CAP without a per-operation guarantee.", "Deleting physical values immediately.", "Ignoring rebalance traffic and hot keys."], followUps: ["How are range scans added?", "How do hinted writes and repair interact?", "How is a stale coordinator fenced?"], variants: ["Values are 10 MB.", "Range scans are required.", "Writes must remain available during a partition.", "One key gets 30% of reads."], remember: "Partitioning chooses owners; replication chooses copies; consistency decides which copies must participate. State each boundary separately.", coreProblem: "Durable partitioned records with explicit consistency.", keyTradeoff: "Availability and latency versus freshness, coordination, and repair.", selfCheck: ["Why do tombstones exist?", "How does a node rejoin safely?", "When are conflicts possible?", "What does a quorum actually guarantee?"], deepDiveOptions: ["Partitioning", "Replication", "Quorums", "Repair", "Compaction"]
  }),
  infra({
    id: "kafka-platform", title: "Kafka-like Streaming Platform", summary: "Append ordered partition logs, replicate durable history, and let independent consumer groups advance offsets.", prompt: "Design a Kafka-like distributed event streaming platform.", difficulty: "advanced", category: "Infrastructure", estimatedMinutes: 40, roleRelevance: ["infrastructure", "data", "backend"], levelRelevance: levels, concepts: ["requirements", "kafka", "partitions", "consumer-groups", "backpressure"], prerequisites: ["event-streaming", "partitions", "consumer-groups", "replication"], clarifyingQuestions: ["Retention by time/size and expected throughput?", "Ordering key and partition count?", "Consumer replay and delivery guarantees?"], functionalRequirements: ["Create topics and append keyed records.", "Read ordered partition ranges by offset.", "Coordinate consumer groups and retain data for replay."], nonFunctionalRequirements: ["Durable acknowledged appends.", "High sequential throughput.", "Ordering only within a partition and bounded recovery time."], outOfScope: commonOut, capacity: { assumptions: ["2 million records/s", "1 KB average record", "24-hour retention", "Replication factor 3"], arithmetic: ["2M × 1 KB ≈ 2 GB/s ingress", "2 GB/s × 86,400 ≈ 173 TB/day logical", "RF3 ≈ 519 TB stored before compression and overhead"], decision: "Sequential segment files, batching, partition parallelism, and retention tiers dominate the architecture." }, apis: [{ method: "POST", path: "/v1/topics/{topic}/records", purpose: "Append batched records with keys." }, { method: "GET", path: "/v1/topics/{topic}/partitions/{p}?offset=...", purpose: "Fetch a sequential range." }, { method: "POST", path: "/v1/groups/{group}/offsets", purpose: "Commit processed positions." }], dataModel: [{ entity: "LogSegment", fields: "topic, partition, base_offset, max_offset, file, checksum", notes: "Immutable sequential segments support retention and replication." }, { entity: "GroupOffset", fields: "group, topic, partition, committed_offset, generation", notes: "Generation fences stale group members." }], simpleDesign: ["One broker appends each topic to a file.", "Consumers fetch by numeric offset.", "Retention deletes old closed segments."], simplePath: `flowchart LR\n  P[Producer] --> B[Single broker]\n  B --> L[(Append-only log)]\n  L --> C[Consumer by offset]`, scaledPath: `flowchart LR\n  P[Producers] --> M[Metadata and partition routing]\n  P --> L1[Partition leaders]\n  L1 -. replicate .-> F[In-sync followers]\n  L1 --> G1[Consumer group A]\n  L1 --> G2[Consumer group B]\n  O[(Group offsets)] --> G1\n  O --> G2`, criticalFlows: [{ title: "Produce", steps: ["Batch and route record by key/partition.", "Leader appends and replicas copy in order.", "Acknowledge according to durability policy."] }, { title: "Consume", steps: ["Group assigns partitions to members.", "Members fetch sequential ranges.", "Commit offset after the chosen processing boundary."] }], bottlenecks: ["One partition limits ordered throughput.", "Too many partitions inflate metadata and recovery work.", "Slow consumers extend retention and disk pressure."], scalingSteps: ["Partition by a stable key and batch producer requests.", "Spread leaders and replicas across failure domains.", "Tier old segments or enforce retention while monitoring consumer lag."], failures: [{ failure: "Partition leader fails", impact: "Writes pause and some acknowledged tail may be at risk under weak policy.", detection: "Heartbeat/quorum and replica lag.", mitigation: "Elect an in-sync follower and fence old leader by epoch.", tradeoff: "Requiring in-sync acknowledgements increases latency or reduces availability." }, { failure: "Consumer processes then crashes before offset commit", impact: "Records are processed again.", detection: "Offset lags behind external effect.", mitigation: "Idempotent effect or atomic offset/effect where one system supports it.", tradeoff: "End-to-end exactly-once does not extend automatically to arbitrary services." }, { failure: "Consumer group rebalance", impact: "Partitions pause and stale members may continue briefly.", detection: "Generation change and heartbeat expiry.", mitigation: "Generation fencing and cooperative assignment.", tradeoff: "Coordination adds pauses and complexity." }], decisions: [{ option: "More partitions", chooseWhen: "Parallel throughput is required.", cost: "Ordering scope shrinks and operations grow." }, { option: "Long retention", chooseWhen: "Replay and independent consumers are valuable.", cost: "Disk and recovery capacity." }, { option: "Synchronous replica acknowledgements", chooseWhen: "Durability of acknowledged records matters.", cost: "Latency and reduced availability during replica loss." }], deepDives: ["Partition append path", "Leader replication", "Consumer group offsets"], mistakes: ["Promising topic-wide order across partitions.", "Adding thousands of partitions without recovery cost.", "Calling broker exactly-once an end-to-end guarantee."], followUps: ["How are partitions rebalanced across brokers?", "How does log compaction differ from retention?", "How do transactions span partitions?"], variants: ["Records must be retained for one year.", "A single key dominates traffic.", "Consumers need replay from last month.", "Cross-region replication is required."], remember: "Kafka-like throughput comes from batching sequential partition logs. Every ordering and parallelism claim must name a partition boundary.", coreProblem: "Durable replayable ordered partition logs.", keyTradeoff: "Ordering and durability versus partition parallelism and availability.", selfCheck: ["Why is offset separate from record storage?", "What makes a follower safe to promote?", "How do partition count and ordering interact?", "Why can consumers repeat effects?"], deepDiveOptions: ["Produce path", "Replication", "Consumer groups", "Retention", "Rebalancing"]
  }),
  infra({
    id: "search-engine", title: "Search Engine", summary: "Continuously crawl and index documents while serving distributed ranked queries from an immutable, replaceable index.", prompt: "Design a web-scale search engine covering crawl, indexing, query serving, and ranking boundaries.", difficulty: "advanced", category: "Infrastructure", estimatedMinutes: 38, roleRelevance: ["infrastructure", "data", "backend"], levelRelevance: levels, concepts: ["requirements", "full-text-search", "elasticsearch", "sharding", "caching"], prerequisites: ["web-crawling", "full-text-search", "inverted-indexes", "sharding"], clarifyingQuestions: ["Web crawl or provided document corpus?", "Keyword retrieval only or personalized ranking?", "Index freshness and query latency targets?"], functionalRequirements: ["Ingest/crawl and process documents.", "Build a searchable inverted index.", "Execute ranked queries and return snippets."], nonFunctionalRequirements: ["Low online query latency.", "Index can be rebuilt and rolled back.", "Freshness is measured separately from serving availability."], outOfScope: ["Secret proprietary ranking formula", "Ad auction", "Browser implementation"], capacity: { assumptions: ["10 billion documents", "100 KB average raw document", "100 thousand queries/s peak"], arithmetic: ["10B × 100 KB ≈ 1 PB raw corpus", "At 20 index shards, peak averages 5K query fan-outs/s per shard before replication", "A 10% daily recrawl means roughly 11.6K documents/s"], decision: "Indexing and query serving require separate pipelines; query shards fan out only to the partitions needed by the index layout." }, apis: [{ method: "POST", path: "/v1/documents", purpose: "Submit a versioned document to indexing." }, { method: "GET", path: "/v1/search?q=...&cursor=...", purpose: "Retrieve ranked results and snippets." }], dataModel: [{ entity: "Document", fields: "doc_id, source_url, version, content_ref, metadata, indexed_at", notes: "Raw/processed corpus is authoritative for rebuild." }, { entity: "Posting", fields: "term, shard, doc_id, term_frequency, positions", notes: "Inverted index maps terms to candidate documents." }], simpleDesign: ["Process a supplied document corpus into one inverted index.", "Lookup postings for query terms.", "Intersect/score candidates and return top results."], simplePath: `flowchart LR\n  D[Documents] --> P[Parser]\n  P --> I[(Inverted index)]\n  Q[Query] --> S[Search service]\n  S --> I`, scaledPath: `flowchart TB\n  subgraph Indexing\n    C[Crawler or ingest] --> P[Document processing]\n    P --> B[Index builder]\n    B --> I[(Versioned index shards)]\n  end\n  subgraph Serving\n    Q[Query] --> F[Query frontend]\n    F --> S[Shard fan-out]\n    S --> I\n    S --> R[Rank and merge]\n  end`, criticalFlows: [{ title: "Index", steps: ["Canonicalize and version documents.", "Tokenize and build immutable shard segments.", "Validate and atomically publish index generation."] }, { title: "Query", steps: ["Parse query and retrieve posting candidates.", "Each shard computes local top-K.", "Coordinator merges, ranks, and generates snippets."] }], bottlenecks: ["Querying raw documents cannot meet latency.", "Every query fan-out consumes shard CPU and tail latency.", "Frequent tiny updates fragment index segments."], scalingSteps: ["Build immutable index segments and merge them in background.", "Shard by document or term based on query/write trade-offs and replicate serving shards.", "Cache popular query/results carefully and use early termination/top-K pruning."], failures: [{ failure: "Bad index generation publishes", impact: "Queries lose or corrupt results.", detection: "Canary recall, size, and document-count validation.", mitigation: "Atomic generation pointer and rollback to prior index.", tradeoff: "Fresh documents disappear until rebuild." }, { failure: "One query shard is slow", impact: "Fan-out tail delays whole response.", detection: "Per-shard latency and partial-result metadata.", mitigation: "Hedged replica request or deadline with explicit partial result.", tradeoff: "Extra load or reduced recall." }, { failure: "Index pipeline lags", impact: "New/updated documents remain stale.", detection: "Document-to-index watermark.", mitigation: "Serve last complete index and prioritize urgent updates.", tradeoff: "Availability wins over freshness." }], decisions: [{ option: "Document sharding", chooseWhen: "Each shard can independently score all query terms.", cost: "Every query fans out to many shards." }, { option: "Term sharding", chooseWhen: "Term postings are enormous and selective routing helps.", cost: "Multi-term queries require cross-shard joins." }, { option: "Immutable segments", chooseWhen: "Safe publication and efficient reads matter.", cost: "Background merges and delayed update visibility." }], deepDives: ["Inverted index", "Distributed top-K", "Index build/publication"], mistakes: ["Combining crawler, indexer, and online serving into one path.", "Claiming a search index is authoritative content storage.", "Ignoring fan-out tail latency."], followUps: ["How are deletions applied?", "How is freshness improved for breaking news?", "How are snippets generated?"], variants: ["Only a company document corpus is indexed.", "Breaking news must appear in one minute.", "Queries require typo tolerance.", "Personalized ranking is added."], remember: "The offline/continuous indexing system builds a replaceable retrieval structure; the online path retrieves and merges bounded candidates.", coreProblem: "Build and serve a distributed inverted index.", keyTradeoff: "Index freshness and query recall versus serving latency and rebuild simplicity.", selfCheck: ["Why use an inverted index?", "What makes an index generation safe to publish?", "Where does query tail latency come from?", "How do local top-K results become global top-K?"], deepDiveOptions: ["Crawling", "Index construction", "Query fan-out", "Ranking", "Freshness"]
  }),
] as const;
