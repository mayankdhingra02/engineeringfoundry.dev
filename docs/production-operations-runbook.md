# Production operations runbook

This runbook is the owner-operated half of P0.1. Repository qualification proves code, migrations, RLS, and local behavior; it does not prove a DNS provider, hosted Supabase project, email delivery, scheduler, backup policy, or deployment environment.

## Ownership and release record

For every production release, record the deployment owner, UTC time, Git SHA, hosting deployment URL, Supabase project reference, final migration filename, and links to the CI runs. Do not place export files, passwords, tokens, service-role keys, reminder secrets, or customer data in the release record.

## Environment contract

Set these in the hosting provider's encrypted environment store; never commit values or print them in CI.

| Variable | Location | Required for | Verification |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_SITE_URL` | public build environment | canonical URLs and production accounts | exact `https://<production-domain>`, no trailing slash; production account UI is unavailable if absent or invalid |
| `NEXT_PUBLIC_SUPABASE_URL` | public build environment | accounts | exact hosted project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | public build environment | accounts | publishable/anon key only, never service role |
| `NEXT_PUBLIC_ACCOUNTS_ENABLED` | public build environment | accounts | keep `false` until hosted qualification passes; then set exactly `true` and redeploy |
| `NEXT_PUBLIC_GOOGLE_AUTH_ENABLED`, `NEXT_PUBLIC_GITHUB_AUTH_ENABLED` | public build environment | optional OAuth | set `true` only after that provider works against the production callback |
| `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` | public build environment | optional analytics | confirm privacy configuration and a public pageview; absence intentionally disables analytics |
| `NEXT_PUBLIC_CONTACT_EMAIL` | public build environment | optional contact email | verified mailbox with a named owner |
| `SUPABASE_SERVICE_ROLE_KEY` | server-only secret store | deletion, reminder worker | never browser-visible; delete control stays unavailable when absent |
| `REMINDER_WORKER_SECRET` | server-only secret store | reminder worker | high-entropy secret; endpoint returns `401` when absent/incorrect |

`REMINDER_EMAIL_PROVIDER` is reserved only. No provider adapter exists, so setting it must not be treated as enabling email reminders.

Before enabling accounts, inspect `.next/static` after a production build and confirm `SUPABASE_SERVICE_ROLE_KEY` and `REMINDER_WORKER_SECRET` are absent. The automated production-hardening regression also verifies server-only usage in source.

## Domain, HTTPS, and Supabase Auth

1. Configure DNS and TLS with the selected hosting provider. Do not enable account traffic over a non-HTTPS production origin.
2. Set `NEXT_PUBLIC_SITE_URL` to that exact origin and deploy.
3. In Supabase Auth URL Configuration, set **Site URL** to the same origin.
4. Add these exact redirect URLs—no production wildcards:
   - `https://<production-domain>/auth/callback`
   - `https://<production-domain>/reset-password` is an application page, but recovery emails must redirect through `/auth/callback?next=/reset-password&flow=recovery`.
5. For any intentionally supported preview domain, add its `/auth/callback` URL individually; otherwise do not authorize it.
6. Configure production SMTP, confirmation, email-change, and recovery templates in Supabase. The app does not claim delivery until a real mailbox test passes.
7. For each enabled OAuth provider, configure its provider-console callback and Supabase provider settings, then complete an actual sign-in. Keep its public flag false until then.

The callback and all post-auth destinations use `safeInternalPath`; arbitrary external `next` values, protocol-relative URLs, and backslash paths fall back to `/dashboard`.

## Database release and recovery

**Owner action required; do not run a local reset against a hosted project.**

1. Confirm point-in-time recovery or scheduled backups are enabled in Supabase, and take a manual backup before the first migration or schema-changing release.
2. `supabase link --project-ref <production-project-ref>`
3. `supabase db diff --linked`; stop on any unexpected drop, rename, or unrelated change.
4. `supabase db push`
5. `supabase migration list --linked`; confirm all **52** migrations are applied through `202609040018_create_ml_design_attempts.sql`.
6. Verify RLS/grants/RPCs, including profiles, applications/rounds, Behavioral stories and answers, DSA/System Design progress and active-plan preferences, ML Design and Behavioral preparation activity, reminders/calendar, Playbook inputs, mock reviews, and Interview Experiences. For profiles, use a disposable owner and the publishable client to call `save_profile_if_revision` with the current revision and canonical professional URLs `https://github.com/qualification-a` and `https://www.linkedin.com/in/qualification-a`; confirm one returned row, a newer revision, the exact stored snapshot, and the safe public projection. Race two full saves from one revision and confirm exactly one coherent winner. Race a full save with `set_profile_display_name` and confirm the desired display name survives while the other fields remain a coherent snapshot. Confirm stale and foreign revisions return zero rows, `anon` cannot execute either writer, and authenticated direct profile updates fail with SQLSTATE `42501`. Confirm changed links using a deceptive host or credentials fail with SQLSTATE `23514`. Inspect the deployed `get_public_profile` definition to confirm unsafe legacy professional-link values are returned as `null` while exact HTTPS GitHub/LinkedIn host aliases remain readable. If an existing pre-migration invalid value is available, verify the public RPC masks it and an unrelated revision save preserves the stored invalid link without rewriting it; do not create, backfill, or rewrite production profile data solely to manufacture this case. For Interview Experiences, confirm the approved-row policies apply only to `anon`, anonymous clients have no table-wide `SELECT`, the safe nested report/round projection succeeds, and author, moderation, lifecycle, round identifier/position, and round process-note columns remain unavailable to anonymous clients. For interview-preparation checklists, use a disposable owner and the publishable client to call `set_interview_preparation_checklist_item`: set two distinct canonical items to `true`, repeat one `true` request, then set only the other item to `false`; confirm independent membership, idempotence, and preservation of the untouched item. Confirm another disposable user cannot change that round. For custom preparation tasks, call `set_interview_preparation_task_completed` twice concurrently with the same explicit `true` target and confirm both calls return the exact task, round, application, and desired state while the task remains complete. Repeat the saved value and confirm its timestamp does not churn; then confirm foreign, missing, and round-mismatched tasks return zero rows without mutation. Confirm `anon` cannot execute the desired-state or retired RPC and that an authenticated legacy `toggle_interview_preparation_task` call fails without mutation with SQLSTATE `0A000` and `Desired-state preparation task saving is required`. For preparation text, confirm `anon` cannot execute `save_interview_preparation_notes_if_revision` or `save_interview_preparation_reflection_if_revision`. Create notes with an absent revision, race two notes saves from the returned revision, and confirm exactly one wins while checklist state remains unchanged. After completing the round, do the same with complete four-field reflection snapshots and confirm no mixed result. Concurrent notes and reflection saves must both succeed in their independent revision domains. Confirm stale, foreign, and missing calls return zero rows without mutation. Confirm the authenticated legacy `save_interview_preparation` signature fails without mutation with SQLSTATE `0A000` and `Revision-checked preparation text saving is required`. For DSA quick progress, confirm `anon` cannot execute `set_dsa_question_quick_progress` while an authenticated disposable owner can. Against one canonical question with existing confidence and notes, issue overlapping desired-status and desired-bookmark calls and confirm both requested fields survive without changing confidence or notes; repeat each desired value and confirm idempotence. Confirm a `false` bookmark request on an untouched canonical question returns that question ID without creating a row, another owner cannot read or mutate the first owner's state, and the RPC rejects both zero and two supplied quick values with SQLSTATE `23514` and `Exactly one quick progress value is required`. For revision-checked DSA full progress, confirm `anon` cannot execute `save_dsa_question_progress_if_revision`, another owner cannot read or change the first owner's row, and a returned `updated_at` advances the revision. Race two full saves from one revision and confirm exactly one coherent snapshot wins; race full saves with quick status and bookmark writes and confirm the quick desired fields survive; race an `absent` full save with `import_dsa_question_progress_if_absent` and confirm neither winner is overwritten. Confirm a stale timestamp or stale absent sentinel returns zero rows without mutation. Confirm the legacy authenticated `save_dsa_question_progress` signature fails with SQLSTATE `0A000` and `Revision-checked DSA progress saving is required`. For revision-checked System Design item progress, confirm `anon` cannot execute `save_system_design_item_progress_if_revision` or `set_system_design_item_quick_progress`. With a disposable owner and one exact `(item_type,item_id)` key, confirm one absent full save succeeds and returns a revision; race two full saves from that revision and confirm exactly one coherent status/confidence/bookmark/note snapshot wins; race a full save with a desired-status quick save and confirm the quick status survives without mixing or clearing the rich fields; repeat the same quick status and confirm the revision does not churn. Race an explicit absent full save against both `import_system_design_item_progress_if_absent` and the desired-status quick RPC, confirm no winner is silently overwritten, and verify a second owner cannot observe or mutate the first owner's row. Confirm the authenticated legacy `save_system_design_item_progress` signature fails without mutation with SQLSTATE `0A000` and `Revision-checked System Design progress saving is required`. For the atomic browser import, confirm `anon` cannot execute `import_dsa_question_progress_if_absent`, `import_system_design_item_progress_if_absent`, or `import_preparation_track_progress_if_absent`, while an authenticated disposable owner can import canonical missing activity. Confirm each RPC returns `true` only for its insert, returns `false` for an existing row without changing richer status, notes, confidence, bookmark, or timestamps, and remains idempotent under repeated and overlapping calls. Confirm concurrent same-key imports insert once, concurrent different-key imports commute, a concurrent rich DSA or System Design save is not overwritten by the import default, and another owner cannot observe or change the first owner's rows. These are hosted owner-run checks and remain unverified until their evidence is recorded.
7. For Behavioral story aggregates, confirm `anon` cannot execute `create_behavioral_story_with_themes`, `update_behavioral_story_with_themes_if_revision`, `duplicate_behavioral_story_with_themes`, or `delete_behavioral_story_if_revision`. With disposable owners, confirm create saves one parent and its exact controlled themes; two updates from one revision produce exactly one coherent winner; stale, foreign, and missing updates return zero rows without mutation; duplication captures one complete parent/theme snapshot; and invalid themes roll back both halves. Race the owner-scoped nested story edit read against an aggregate update and confirm the edit snapshot is wholly before or wholly after the update, with its parent revision and complete theme set from the same committed state. Race a full story save against an exact-revision delete and confirm exactly one wins: a winning save preserves its coherent parent/theme snapshot, while a winning delete removes the parent and cascades themes and links. Confirm stale deletion preserves the parent, themes, links, and linked answer provenance; foreign and missing deletes return the same zero-row result. Confirm authenticated direct story `INSERT`/`UPDATE`/`DELETE` and theme `INSERT`/`UPDATE`/`DELETE` fail, and the authenticated legacy `replace_behavioral_story_themes` call fails with SQLSTATE `0A000` and `Atomic Behavioral story saving is required`. These hosted checks remain unverified until their evidence is recorded.
8. For Behavioral answer aggregates, confirm `anon` cannot execute `create_behavioral_answer_aggregate`, `update_behavioral_answer_aggregate_if_revision`, or `delete_behavioral_answer_if_revision`. With disposable owners and one question, confirm create saves content and desired primary state together; race two full edits from one revision and confirm exactly one coherent snapshot wins; then race two answers requesting primary and confirm exactly one requested answer is primary. Race one full answer save against an exact-revision delete and confirm exactly one wins; a stale or question-mismatched delete must preserve content and primary state. Confirm stale revisions, foreign answer targets, missing answer targets, and question-mismatched targets return zero rows without changing answer content or the prior primary, and invalid input rolls back before any primary change. Confirm authenticated direct answer `INSERT`/`UPDATE`/`DELETE` fail while owner `SELECT` remains available, and the authenticated legacy `set_behavioral_primary_answer` call fails without mutation with SQLSTATE `0A000` and `Atomic Behavioral answer saving is required`. These hosted checks remain unverified until their evidence is recorded.
9. For custom Behavioral questions, confirm `anon` cannot execute `save_behavioral_custom_question_if_revision` or `delete_behavioral_custom_question_if_revision`. With one disposable owner and caller-generated question ID, confirm an absent create returns one correlated row, an absent replay returns zero without mutation, and the returned revision advances after an exact full save. Race two complete snapshots from one revision and confirm exactly one coherent question/context/category/company/private-note set wins. Confirm stale parent-revision, foreign, and missing saves and deletes are indistinguishable zero-row outcomes and preserve the question and linked preparation; confirm exact-revision delete intentionally cascades its private children. Authenticated direct question `INSERT`, `UPDATE`, and `DELETE` must fail with SQLSTATE `42501`. These hosted checks remain unverified until their evidence is recorded.
10. For reminder preferences, confirm `anon` cannot execute `save_interview_reminder_preferences_if_revision`. With a disposable owner, submit an explicit absent revision and confirm one complete preference snapshot and its returned `updated_at`; retry the absent revision and confirm zero rows and no mutation. Race two full saves from one loaded revision and confirm exactly one complete preference/reminder snapshot wins, the other returns zero rows, and the successful revision advances. Race a preference save with a future-round schedule change and confirm serialized reminder rows match the final committed preference and round state. Review the candidate's local pgTAP/database evidence that a forced reminder-resync failure rolls the preference snapshot back; do not inject a failure into a hosted project. Confirm another owner cannot read or mutate the preference and a repeated onboarding submission after completed onboarding changes neither reminder nor preparation preferences. Confirm the authenticated legacy `save_interview_reminder_preferences` call fails without mutation with SQLSTATE `0A000` and `Revision-checked reminder preference saving is required`. These safe hosted checks remain unverified until their evidence is recorded.
11. For Interview Playbook diagnostic inputs, confirm `anon` cannot execute `get_interview_playbook_diagnostic_inputs_snapshot` or `save_interview_playbook_diagnostic_inputs_if_revision`. With a disposable owner, confirm the read RPC returns exactly one explicit neutral row before the first save, then save an absent revision and confirm one returned `updated_at` plus canonical confidence order and stored priority/constraint order. Race two complete saves from the same revision and confirm exactly one coherent aggregate wins; concurrently read during a save and confirm the snapshot is wholly before or wholly after, never a mixed parent/child view. Confirm stale, foreign, and missing revisions are indistinguishable zero-row outcomes without mutation, a second owner cannot observe the first owner's aggregate, and the authenticated legacy `save_interview_playbook_diagnostic_inputs` call fails without mutation with SQLSTATE `0A000` and `Revision-checked Interview Playbook diagnostic saving is required`. These hosted owner-run checks remain unverified until their evidence is recorded.
12. For revision-checked Interview Experiences, confirm `anon` cannot execute `save_interview_experience_if_revision`, `manage_interview_experience_if_revision`, or `moderate_interview_experience_if_revision`. With disposable owners and an operator, create one private aggregate, race two owner saves from one revision and confirm exactly one coherent parent/round snapshot wins. Then race an owner resubmit against moderation from the same revision and confirm only one succeeds: moderation must never approve content the operator did not review. Separately race an owner withdraw or delete against moderation and confirm only one lifecycle change succeeds. Confirm every stale call returns zero rows without mutation, the returned revision advances, foreign and missing targets are indistinguishable, and another owner cannot observe the private aggregate. Confirm authenticated legacy `save_interview_experience_draft`, `submit_interview_experience`, `withdraw_interview_experience`, `delete_interview_experience`, and `moderate_interview_experience` calls fail before mutation with SQLSTATE `0A000` and, respectively, `Revision-checked interview experience saving is required`, `Revision-checked interview experience submission is required`, `Revision-checked interview experience management is required`, and `Revision-checked interview experience moderation is required`. These hosted owner-run checks remain unverified until their evidence is recorded.
13. Run the two-disposable-account qualification in `docs/production-launch-checklist.md`, including account export schema `1.5`, account deletion, P0.2 preparation activity and active-plan isolation, the atomic browser-import no-overwrite boundary, mock review isolation, private Interview Experience draft isolation, and P0.8 feedback/admin operations.
14. For Application tracker deletion, confirm `anon` cannot execute `delete_application_if_revision` or `delete_interview_round_if_revision`, authenticated direct application and round `DELETE` statements fail with SQLSTATE `42501`, and another owner receives the same zero-row result for a foreign target as a missing target. Advance an application's revision, then confirm its stale delete returns zero and preserves the newer row. Repeat for an interview round. Finally, use each exact current revision to delete a disposable round and application, and confirm application deletion performs the documented account-owned cascades. These hosted owner-run checks remain unverified until their evidence is recorded.

Apply `202609040015_publish_system_design_production_engineering.sql` before deploying the 18 Production Engineering lessons. After migration, confirm the public catalog contains the exact 164 published concepts and that a disposable authenticated owner can save and clear progress for one new canonical concept such as `observability`. An application-first deployment leaves the lessons publicly readable but makes account progress fail for the new IDs until migration; a code rollback after migration is safe because the additive catalog rows are read-only and existing private progress remains protected by the catalog foreign key. Keep the migration and roll forward.

Apply `202609040016_publish_system_design_common_patterns.sql` before deploying the 14 Common Architecture Patterns lessons. After migration, confirm the public catalog contains exactly 178 published concepts and that a disposable authenticated owner can save and clear progress for one new canonical concept such as `scaling-reads`. An application-first deployment leaves lessons publicly readable but makes account progress fail for the 14 new IDs until migration. A code rollback is safe with the additive, read-only catalog rows retained; do not delete rows that may already anchor private progress.

Apply `202609040017_publish_system_design_required_closure.sql` before deploying the ten Required System Design closure lessons. After migration, confirm the public catalog contains exactly 188 published concepts and that a disposable authenticated owner can save and clear progress for one new canonical concept such as `payments-ledgers`. An application-first deployment leaves lessons publicly readable but makes account progress fail for the ten new IDs until migration. A code rollback is safe with the additive, read-only catalog rows retained; do not delete rows that may already anchor private progress.

Apply `202609040018_create_ml_design_attempts.sql` before enabling saved ML Design attempts. With two disposable owners, confirm each can create and read only their own attempt, a stale revision cannot overwrite or delete a newer worksheet, direct client writes remain denied, and anonymous RPC execution fails. The public tab-only rehearsal remains available if application code lands first; account saves fail until migration. A code rollback is safe with owner-scoped attempt rows retained, so keep the additive migration and roll forward.

15. For System Design attempts, confirm `anon` cannot execute `delete_system_design_attempt_if_revision` or the retired `delete_system_design_attempt`, while an authenticated legacy call fails without mutation with SQLSTATE `0A000` and `Revision-checked System Design attempt deletion is required`. Race one complete attempt save against an exact-revision delete for the same owner, problem, and attempt; confirm exactly one returns a row, a winning save preserves the coherent worksheet and increments its revision, and a winning delete removes only the original revision. Confirm stale and problem-mismatched revisions preserve the attempt, foreign and missing targets return indistinguishable zero rows, and direct table deletion remains denied. These hosted owner-run checks remain unverified until their evidence is recorded.
16. For custom preparation-task deletion, confirm `anon` cannot execute `delete_interview_preparation_task_if_revision` or the retired `delete_interview_preparation_task`. Race desired completion against exact-revision deletion for one owned task and confirm exactly one returns a correlated row: a winning completion preserves the task with the desired state and a newer revision, while a winning delete removes only the original revision. Confirm stale and round-mismatched deletes preserve the task, foreign and missing targets return indistinguishable zero rows, authenticated direct task `DELETE` is denied, and the authenticated legacy delete fails without mutation with SQLSTATE `0A000` and `Revision-checked preparation task deletion is required`. These hosted owner-run checks remain unverified until their evidence is recorded.
17. For account preparation preferences, confirm `anon` cannot execute `save_account_preparation_preferences_if_revision`. With a disposable owner, create from the explicit absent revision and confirm one returned `updated_at`; replay absent and confirm zero rows without mutation. Race two complete role/focus/DSA-roadmap snapshots from one revision and confirm exactly one coherent winner. Then race a full settings snapshot with a distinct desired DSA-roadmap update and confirm the newer DSA intent survives while role, focus, and every unrelated preparation-planning field remain coherent. Confirm stale and foreign revisions return zero rows, another owner cannot observe the row, and the authenticated legacy `save_account_preparation_preferences` call fails without mutation with SQLSTATE `0A000` and `Revision-checked preparation preference saving is required`. These hosted owner-run checks remain unverified until their evidence is recorded.

Apply `202609030005_save_behavioral_story_aggregate.sql` before the aggregate-writing application. Migration-first closes authenticated direct story/theme mutation and makes already-loaded old split-write clients fail safely; app-first leaves their torn-write window open until the migration lands. A post-migration rollback to old application code is safe but degraded because those old writes remain unavailable, so retain the migration and roll forward.

Apply `202609030006_save_system_design_item_progress_if_revision.sql` before the revision-aware System Design application. Migration-first makes already-loaded whole-row clients fail safely with SQLSTATE `0A000`; application-first protects only newly loaded clients and leaves the stale overwrite window open until the migration lands. A post-migration application rollback is safe but degraded because the legacy full-save boundary remains unavailable, so retain the migration and roll forward.

Apply `202609030007_save_behavioral_answer_aggregate.sql` before the Behavioral answer aggregate application. Migration-first closes authenticated direct answer `INSERT`/`UPDATE` and makes the legacy primary-only RPC fail with SQLSTATE `0A000`, so already-loaded split-write clients fail before partial mutation; application-first protects only newly loaded clients and leaves the old torn-save and stale-overwrite window open until the migration lands. A post-migration application rollback is safe but degraded because the legacy direct and primary-only paths remain unavailable, so retain the migration and roll forward.

Apply `202609040001_save_interview_reminder_preferences_if_revision.sql` before the revision-aware reminder-settings application. Migration-first makes already-loaded full-snapshot clients fail safely with SQLSTATE `0A000` and prevents repeated completed-onboarding requests from bypassing the revision boundary; application-first protects only newly loaded settings clients and leaves the stale overwrite window open until the migration lands. A post-migration application rollback is safe but degraded because the legacy preference-save path remains unavailable, so retain the migration and roll forward.

Apply `202609040002_save_interview_playbook_diagnostic_inputs_if_revision.sql` before the revision-aware Interview Playbook application. Migration-first makes already-loaded whole-aggregate clients fail safely with SQLSTATE `0A000` and supplies the coherent snapshot read before the new application depends on it; application-first can break new reads while leaving old clients able to overwrite newer diagnostic inputs until the migration lands. A post-migration application rollback is safe but degraded because the legacy snapshot-save boundary remains unavailable, so retain the migration and roll forward.

Apply `202609040003_save_interview_experience_if_revision.sql` before deploying the revision-aware Interview Experience application and moderation UI. Migration-first makes already-loaded legacy save, submit, withdraw, delete, and moderation clients fail safely with SQLSTATE `0A000` before mutation; application-first protects only newly loaded clients and leaves stale aggregate and moderation overwrites possible until the migration lands. A post-migration application rollback is safe but degraded because the legacy mutations remain unavailable, so retain the migration and roll forward.

Apply `202609040004_save_interview_preparation_text_if_revision.sql` before deploying revision-aware preparation notes and reflection forms. Migration-first prevents already-loaded legacy clients from performing stale whole-snapshot writes; they fail safely with SQLSTATE `0A000`. Application-first protects only newly loaded forms and leaves the legacy overwrite path open until the migration lands. A code rollback after migration is safe but degraded because legacy preparation text saves remain unavailable; keep the migration and roll forward.

Apply `202609040005_set_interview_preparation_task_completed.sql` before deploying the desired-state custom-task action. Migration-first makes already-loaded flip-current clients fail safely with SQLSTATE `0A000`, preventing duplicate stale intentions from inverting the saved state. Application-first protects only newly loaded controls and leaves the legacy toggle behavior available until the migration lands. A code rollback after migration is safe but degraded because old task toggles remain unavailable; keep the migration and roll forward.

Apply `202609040006_save_profile_if_revision.sql` before deploying revision-aware profile settings. Migration-first denies already-loaded direct profile writers with SQLSTATE `42501`; the new full-profile CAS and one-field display-name RPC then serialize under the same owner lock. Application-first makes new saves fail safely while the RPCs are absent but leaves older stale full writes possible until the migration lands. A code rollback after migration is safe but degraded because legacy direct profile updates remain unavailable; keep the migration and roll forward.

Apply `202609040007_save_behavioral_custom_question_if_revision.sql` before deploying revision-aware custom Behavioral questions. Migration-first denies already-loaded direct question `INSERT`, `UPDATE`, and `DELETE` with SQLSTATE `42501`; the new owner-derived save and delete RPCs share one per-question lock and exact revision. Application-first makes new question mutations fail safely while the RPCs are absent but leaves old stale direct writes available until migration. A code rollback after migration is safe but degraded because direct question mutations remain unavailable; keep the migration and roll forward.

Apply `202609040008_delete_application_tracker_if_revision.sql` before deploying revision-aware Application tracker deletion. Migration-first makes already-loaded direct application and round deletes fail safely with SQLSTATE `42501`; the new client can then use only the owner-derived exact-revision RPCs. Application-first makes new deletes fail safely while the RPCs are absent but leaves already-loaded direct clients able to delete a newer record until the migration lands. A code rollback after migration is safe but degraded because legacy direct deletes remain unavailable; keep the migration and roll forward.

Apply `202609040009_delete_behavioral_records_if_revision.sql` before deploying revision-aware Behavioral story and answer deletion. Migration-first makes already-loaded direct story and answer deletes fail safely with SQLSTATE `42501`; the new client can then use only the owner-derived exact-revision RPCs that share the existing story and question locks. Application-first makes new deletes fail safely while the RPCs are absent but leaves already-loaded direct clients able to delete newer Behavioral records until the migration lands. A code rollback after migration is safe but degraded because legacy direct deletes remain unavailable; keep the migration and roll forward.

Apply `202609040010_delete_system_design_attempt_if_revision.sql` before deploying revision-aware System Design attempt deletion. Migration-first makes already-loaded clients calling `delete_system_design_attempt` fail without mutation with SQLSTATE `0A000`; the new save and delete paths then share one owner/attempt lock and require the exact displayed revision. Application-first makes new deletes fail safely while the RPC is absent but leaves the legacy stale-delete path available until migration. A code rollback after migration is safe but degraded because legacy attempt deletion remains unavailable; keep the migration and roll forward.

Apply `202609040011_delete_interview_preparation_task_if_revision.sql` before deploying revision-aware custom preparation-task deletion. Migration-first makes already-loaded clients calling `delete_interview_preparation_task` fail without mutation with SQLSTATE `0A000`; desired completion and exact-revision deletion then share one owner/task lock. Application-first makes new deletes fail safely while the RPC is absent but leaves the legacy stale-delete path available until migration. A code rollback after migration is safe but degraded because legacy task deletion remains unavailable; keep the migration and roll forward.

Apply `202609040012_update_feedback_submission_if_revision.sql` before deploying revision-aware feedback triage. Migration-first makes already-loaded triage clients fail without mutation with SQLSTATE `0A000` and `Revision-checked feedback triage is required`; the new status-and-private-note writer then requires the exact displayed revision under one feedback-item lock. Race two complete feedback triage snapshots from one displayed revision in hosted owner verification and require exactly one coherent winner, with stale/no-op/audit/grant behavior matching the launch checklist; leave that check unverified until evidence is recorded. Application-first makes new saves fail safely while the RPC is absent but leaves the legacy overwrite path available until migration. A code rollback after migration is safe but degraded because legacy triage remains unavailable; keep the migration and roll forward.

Apply `202609040013_harden_interview_preparation_task_add.sql` before deploying the strict custom-task action. Migration-first serializes every `add_interview_preparation_task` caller per owner/round before enforcing the 12-task cap and assigning positions; application-first rejects malformed browser input but leaves concurrent direct or already-loaded callers on the legacy count-then-insert race until migration. The RPC signature remains compatible, so rolling application code back after migration is safe; keep the migration and roll forward. In hosted verification, fill one disposable round to 11 tasks, race two additions, and confirm exactly one succeeds, the other fails with SQLSTATE `P0001` and `Custom task limit reached`, and the final 12 rows have distinct positions 0 through 11. Also confirm `anon` and direct inserts fail with `42501`, malformed titles fail with `23514`, and foreign and missing rounds both fail with `P0002`. Leave these hosted checks unverified until evidence is recorded.

Apply `202609040014_save_account_preparation_preferences_if_revision.sql` before deploying revision-aware preparation settings. Migration-first makes already-loaded full-snapshot clients fail safely with SQLSTATE `0A000` and `Revision-checked preparation preference saving is required`; the new owner-derived RPC accepts only an explicit absent state or the exact loaded revision. Application-first protects only newly loaded settings clients and leaves the stale legacy overwrite window open until migration. A post-migration application rollback is safe but degraded because legacy settings saves remain unavailable; retain the migration and roll forward.

Migrations are forward-only. If a migration fails before completion, stop the deployment, preserve the error with secrets redacted, compare the linked migration history, and apply a new corrective migration only after review. Do not edit an already-applied migration. Restore from backup only when data loss is accepted; verify restore in a separate environment before routing production traffic.

## Reminder worker

The endpoint is `POST https://<production-domain>/api/internal/reminders/process` with `Authorization: Bearer <REMINDER_WORKER_SECRET>`. An external HTTPS scheduler is required; no provider is assumed by this repository.

- Start with a manual authenticated request and verify `401` for a missing/wrong secret.
- With a valid secret, the current no-provider state must return `503`; do not mark email delivery configured.
- When a provider adapter is deliberately added later, verify provider idempotency, retry classes, scheduler overlap, recipient handling, secret rotation, and logs before enabling email preference.
- In-app reminder state is independent and remains available without email delivery.

## Production smoke checklist

Run after deployment, recording tester/date/evidence without private payloads.

After deploying the exact candidate that completed local qualification, run the automated public-route smoke against that deployment:

```bash
PUBLIC_SMOKE_ORIGIN=https://engineeringfoundry.dev npm run test:public-routes:hosted
```

This is a post-deployment command. It requires a root HTTP(S) origin and rejects paths, query strings, fragments, and embedded credentials. It does not build the application or start/stop a local Next.js server. `npm run test:public-routes` remains the local built-candidate smoke used by `npm run qualify:launch:production`; do not treat that local command as hosted verification. Record the result as dated deployment evidence only after it runs successfully; until then, keep `DEPLOYMENT_STATUS=NOT_DEPLOYED_HOSTED_GATES_PENDING`.

**Public:** homepage, DSA, System Design, ML Design, Behavioral, Interview Playbook, Interview Experiences, companies, and mock interviews; canonical/robots/sitemap; HTTPS headers and no CSP violations on a representative public and private page.

**Auth:** sign-up with confirmation, callback, onboarding, sign-in, sign-out/sign-back-in, password recovery/update, account email change, settings, and each explicitly enabled OAuth provider.

**Private data:** with User A and User B, verify application, Behavioral, DSA/System Design progress and active plans, ML Design/Behavioral preparation activity, mock-review, Interview Experience draft, calendar/reminder, export, and deletion isolation. While signed out, verify browser progress imports only canonical activity after an explicit consent action and never overwrites account records. Confirm approved/consented Interview Experiences are publicly readable while drafts/submitted records are not.

**Analytics:** only if configured, verify a public pageview; navigate private routes and confirm no private pageviews, query strings, tokens, email, notes, stories, answers, application data, mock ratings/notes, or experience draft text arrive in PostHog.

**P0.9 analytics and evidence:** follow `docs/analytics-launch-operations.md` before enabling PostHog. Verify identify/reset after a real sign-in/sign-out, create the four specified dashboards only after their events are observed, and commit the first real aggregate monthly snapshot only after the month closes. `docs/impact-ledger/` templates are not evidence and must never be counted as adoption, testimonials, outcomes, or impact.

## Incident and rollback

1. For account/auth risk, set `NEXT_PUBLIC_ACCOUNTS_ENABLED=false` and redeploy. Public learning remains available; data is retained.
2. For worker risk, unset `REMINDER_WORKER_SECRET`; requests fail closed and no delivery occurs.
3. For deletion/worker credential risk, unset `SUPABASE_SERVICE_ROLE_KEY`; deletion becomes explicitly unavailable.
4. For application defects, redeploy the prior application build. Do not roll back migrations; use a new corrective migration.
5. Preserve only redacted operational logs. Escalate data exposure, authentication bypass, or destructive failure before resuming deployments.

## Repository commands

```bash
npm run qualify:static
npm run qualify:database
npm run qualify:launch:production
npm run release:verify
```

The release build is `next build`, a **Next.js production build using Turbopack**. CI and local production qualification use that same command.

These checks are automated in the repository or CI. DNS, SSL, managed backups, SMTP, OAuth provider settings, Supabase hosted migration application, PostHog configuration, scheduling, and deployment are owner/hosting-provider actions and block full P0.1 completion until recorded as verified.
