import type { Metadata } from "next";
import { Award, Binary, Github, Linkedin, MessagesSquare, Trophy, UserRound, Users } from "lucide-react";
import { PageHero, SectionHeading, StatusPill } from "@/components/page-shell";

export async function generateMetadata({ params }: { params: Promise<{ username: string }> }): Promise<Metadata> { const { username } = await params; return { title: `@${username} Profile`, description: "Engineering Foundry public profile preview.", robots: { index: false, follow: false } }; }
export default async function ProfilePage({ params }: { params: Promise<{ username: string }> }) { const { username } = await params; return <><PageHero eyebrow="Public profile · Demo" title={`@${username}`} description="Demo Engineer · Software engineering community member"><div className="tag-list"><StatusPill tone="accent">Profile shell</StatusPill><a className="button button-secondary" href="#links"><Github size={15} /> GitHub</a><a className="button button-secondary" href="#links"><Linkedin size={15} /> LinkedIn</a></div></PageHero><section className="section"><div className="page-width"><div className="dashboard-grid"><section><SectionHeading eyebrow="Activity" title="Practice and contribution." /><div className="feature-grid" style={{ gridTemplateColumns:"repeat(2, 1fr)" }}><div className="mini-card"><Binary size={18} /><h3>DSA progress</h3><p>33% of demo foundation track.</p></div><div className="mini-card"><MessagesSquare size={18} /><h3>Mock interviews</h3><p>No completed sessions yet.</p></div><div className="mini-card"><Trophy size={18} /><h3>Challenges</h3><p>Challenge program coming soon.</p></div><div className="mini-card"><Users size={18} /><h3>Referrals contributed</h3><p>No public activity to show.</p></div></div></section><aside className="panel"><div className="company-initial" style={{ width:64,height:64,fontSize:22 }}>DE</div><h2 style={{ marginTop:20 }}>Demo Engineer</h2><p className="muted" style={{ fontSize:13,lineHeight:1.6 }}>This shell supports a bio, company, role, years of experience, and professional links after account setup.</p><div className="activity-row"><strong>Current role</strong><span>Not provided</span></div><div className="activity-row"><strong>Experience</strong><span>Not provided</span></div></aside></div></div></section><section className="section section-alt"><div className="page-width"><SectionHeading eyebrow="Badges" title="Milestones and contribution." /><div className="mini-grid"><div className="mini-card"><Award size={20} /><h3>Founding Member</h3><p>Clearly labeled demo badge.</p></div><div className="empty-state"><UserRound size={21} /><strong>More activity unlocks more context</strong><p>Badge rules and review integrity are deferred.</p></div></div></div></section></>; }
