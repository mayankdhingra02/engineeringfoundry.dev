import type { Metadata } from "next";
import { Award, Medal, Trophy } from "lucide-react";
import { PageHero, SectionHeading, StatusPill } from "@/components/page-shell";

export const metadata: Metadata = { title: "Leaderboard", description: "A future leaderboard for challenges and community contribution." };
const rows = [{ rank: "01", user: "Demo User 01", challenge: 480, completed: 8, contribution: 34, mocks: 6, reviews: 9 }, { rank: "02", user: "Demo User 02", challenge: 445, completed: 7, contribution: 29, mocks: 8, reviews: 7 }, { rank: "03", user: "Demo User 03", challenge: 410, completed: 7, contribution: 38, mocks: 4, reviews: 11 }];
export default function LeaderboardPage() { return <><PageHero eyebrow="Leaderboard" title="Celebrate consistency and contribution." description="A future opt-in leaderboard across challenge performance, practice, review, and community contribution." /><section className="section"><div className="page-width"><SectionHeading eyebrow="Demo data only" title="A balanced view of community activity." description="All identities below are explicitly labeled demo users. Real participation will require an account and opt-in settings." /><div className="data-table"><div className="table-head" style={{ gridTemplateColumns:"55px 1fr repeat(5, 120px)" }}><span>Rank</span><span>Member</span><span>Score</span><span>Challenges</span><span>Contribution</span><span>Mocks</span><span>Reviews</span></div>{rows.map((row, index) => <div className="question-row" style={{ gridTemplateColumns:"55px 1fr repeat(5, 120px)" }} key={row.user}><span>{index === 0 ? <Trophy size={17} /> : index === 1 ? <Medal size={17} /> : <Award size={17} />}</span><strong>{row.user}</strong><span>{row.challenge}</span><span>{row.completed}</span><span>{row.contribution}</span><span>{row.mocks}</span><span>{row.reviews}</span></div>)}</div><p className="muted" style={{ fontSize:12, marginTop:14 }}><StatusPill>Preview</StatusPill> Scoring rules, privacy controls, seasons, and moderation are deferred.</p></div></section></>; }
