export default function ApplicationsLoading() { return <div className="tracker-workspace"><div className="page-width tracker-shell" role="status" aria-live="polite" aria-busy="true"><span className="sr-only">Loading applications…</span><div className="tracker-loading-heading" aria-hidden="true" /><div className="tracker-summary" aria-hidden="true">{Array.from({ length: 4 }, (_, index) => <div className="tracker-loading-card" key={index} />)}</div><div className="tracker-loading-table" aria-hidden="true" /></div></div>; }
